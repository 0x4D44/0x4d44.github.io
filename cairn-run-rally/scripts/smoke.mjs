import { spawn, spawnSync } from 'node:child_process';
import { existsSync } from 'node:fs';
import { mkdir, mkdtemp, rm, writeFile } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';

const root=new URL('../',import.meta.url),port=9333;
await mkdir(new URL('../artifacts/',import.meta.url),{recursive:true});
const candidates=[process.env.CHROME_BIN,'/usr/bin/chromium','/usr/bin/chromium-browser','/usr/bin/google-chrome','/Applications/Google Chrome.app/Contents/MacOS/Google Chrome','C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe'].filter(Boolean);
const chrome=candidates.find(existsSync);
if(!chrome)throw new Error('Chromium or Google Chrome is required for the browser smoke test.');
const profile=await mkdtemp(join(tmpdir(),'cairn-run-smoke-'));
const chromeArgs=['--no-sandbox','--disable-dev-shm-usage','--ignore-gpu-blocklist','--enable-webgl','--allow-file-access-from-files','--autoplay-policy=no-user-gesture-required',`--remote-debugging-port=${port}`,'--remote-debugging-address=127.0.0.1',`--user-data-dir=${profile}`,'--window-size=1280,720','about:blank'];
let command=chrome,args=chromeArgs;
if(process.platform==='linux'&&spawnSync('sh',['-lc','command -v xvfb-run'],{encoding:'utf8'}).status===0){command='xvfb-run';args=['-a',chrome,'--use-gl=angle','--use-angle=swiftshader',...chromeArgs];}else args=['--headless=new',...chromeArgs];
const child=spawn(command,args,{detached:process.platform!=='win32',stdio:['ignore','ignore','pipe']});let stderr='';child.stderr.on('data',d=>stderr+=d);
let ws;
try{
 let version=null;for(let i=0;i<80;i++){try{version=await fetch(`http://127.0.0.1:${port}/json/version`).then(r=>r.ok?r.json():null);if(version)break;}catch{}await new Promise(r=>setTimeout(r,250));}
 if(!version)throw new Error(`Chrome debugging endpoint did not start. ${stderr.slice(-1000)}`);
 const target=await fetch(`http://127.0.0.1:${port}/json/new`,{method:'PUT'}).then(r=>r.json());ws=new WebSocket(target.webSocketDebuggerUrl);let id=0;const pending=new Map(),networkRequests=[];
 ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.method==='Network.requestWillBeSent')networkRequests.push(m.params.request.url);if(m.id&&pending.has(m.id)){const p=pending.get(m.id);pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);}};
 await new Promise((resolve,reject)=>{ws.onopen=resolve;ws.onerror=reject;});
 const send=(method,params={})=>new Promise((resolve,reject)=>{const n=++id;pending.set(n,{resolve,reject});ws.send(JSON.stringify({id:n,method,params}));});
 await send('Page.enable');await send('Runtime.enable');await send('Performance.enable');await send('Network.enable');await send('Emulation.setDeviceMetricsOverride',{width:1280,height:720,deviceScaleFactor:1,mobile:false});
 await send('Page.navigate',{url:new URL('index.html?smoke=1',root).href});
 for(let i=0;i<80;i++){const ready=await send('Runtime.evaluate',{expression:'Boolean(window.__game)',returnByValue:true});if(ready.result.value)break;if(i===79)throw new Error('Browser game did not boot from its real ES module graph.');await new Promise(r=>setTimeout(r,100));}
 await new Promise(r=>setTimeout(r,300));
 await send('Runtime.evaluate',{expression:'localStorage.setItem("cairn-run:save:v1","{broken-json");true',returnByValue:true});await send('Page.reload');
 for(let i=0;i<80;i++){const ready=await send('Runtime.evaluate',{expression:'Boolean(window.__game)',returnByValue:true});if(ready.result.value)break;if(i===79)throw new Error('Browser did not recover from a corrupt save.');await new Promise(r=>setTimeout(r,100));}
 const corruptSave=await send('Runtime.evaluate',{expression:'({mode:window.__game.mode,version:window.__game.session.save.version,championship:window.__game.session.championship,errors:window.__game.errors})',returnByValue:true});
 const shell=await send('Runtime.evaluate',{expression:'({titleVisible:!document.getElementById("title-screen").classList.contains("hidden"),startText:document.getElementById("start-button").textContent,mode:window.__game?.mode})',returnByValue:true});
 const titleLayout=await send('Runtime.evaluate',{expression:'(()=>{const screen=document.getElementById("title-screen"),footer=screen.querySelector("footer");return {clientHeight:screen.clientHeight,scrollHeight:screen.scrollHeight,footerBottom:footer.getBoundingClientRect().bottom,viewport:innerHeight};})()',returnByValue:true});
 const titleShot=await send('Page.captureScreenshot',{format:'png',fromSurface:true});await writeFile(new URL('../artifacts/title.png',import.meta.url),Buffer.from(titleShot.data,'base64'));
 const value=async expression=>(await send('Runtime.evaluate',{expression,returnByValue:true})).result.value;
 const waitForValue=async(expression,predicate,label)=>{let current;for(let i=0;i<180;i++){current=await value(expression);if(predicate(current))return current;await new Promise(r=>setTimeout(r,100));}throw new Error(`${label}: ${JSON.stringify(current)}`);};
 await value('document.getElementById("practice-button").click()');await waitForValue('window.__game.mode',v=>v==='selection','practice did not open selection');
 await value('(()=>{document.querySelector("input[value=lumen-f2]").click();document.querySelector("input[value=aurora-forest]").click();document.getElementById("selection-form").requestSubmit();return true;})()');
 await waitForValue('window.__game.mode',v=>v==='playing','practice selection did not start');
 const selectedPractice=await value('({stage:window.__game.activeRun.stage.id,car:window.__game.activeRun.car.id,world:window.__game.world.region.id})');
 await value('window.__game.returnToTitle()');
 await value('document.getElementById("championship-button").click()');await waitForValue('window.__game.mode',v=>v==='selection','championship did not open selection');
 await value('(()=>{document.querySelector("input[value=cairn-r4]").click();document.querySelector("input[value=hard]").click();document.getElementById("selection-form").requestSubmit();return true;})()');
 await waitForValue('window.__game.mode',v=>v==='service','championship did not reach service');
 await value('(()=>{document.getElementById("service-auto").click();document.getElementById("service-form").requestSubmit();return true;})()');
 await waitForValue('window.__game.mode',v=>v==='playing','first championship event did not start');
 const firstRunId=await value('window.__game.session.championship.runId');await value('window.__game.returnToTitle()');
 const resumeVisible=await value('!document.getElementById("resume-championship").hidden');await value('document.getElementById("resume-championship").click()');await waitForValue('window.__game.mode',v=>v==='playing','saved championship did not resume');
 const resumedRunId=await value('window.__game.session.championship.runId');
 await new Promise(r=>setTimeout(r,350));
 await value('window.__game.finishRun({time:322.61,splits:[{time:94.38},{time:204.37},{time:322.61}]})');await waitForValue('window.__game.mode',v=>v==='results','first championship result did not classify');
 const firstResult=await value('({eventIndex:window.__game.session.championship.eventIndex,rows:document.querySelectorAll("#result-standings tbody tr").length,audioVoices:window.__game.audio.voiceCount()})');
 await value('document.getElementById("result-next").click()');await waitForValue('window.__game.mode',v=>v==='service','next event did not reach service');
 await value('(()=>{document.getElementById("service-auto").click();document.getElementById("service-form").requestSubmit();return true;})()');await waitForValue('window.__game.mode',v=>v==='playing','second championship event did not start');
 const secondSelection=await value('({stage:window.__game.activeRun.stage.id,world:window.__game.world.region.id})');
 await new Promise(r=>setTimeout(r,350));
 await value('window.__game.finishRun({time:330,splits:[{time:110},{time:220},{time:330}]})');await waitForValue('window.__game.mode',v=>v==='results','final championship result did not classify');
 await value('document.getElementById("result-next").click()');await waitForValue('window.__game.mode',v=>v==='standings','classified championship did not show standings');
 const championshipFlow=await value('({phase:window.__game.session.championship.phase,results:window.__game.session.championship.results.length,rows:document.querySelectorAll("#standings-table tbody tr").length,audioVoices:window.__game.audio.voiceCount()})');
 await value('document.getElementById("standings-continue").click()');await waitForValue('window.__game.mode',v=>v==='title','standings did not return to title');
 await value('document.getElementById("settings-button").click()');await waitForValue('window.__game.mode',v=>v==='settings','settings did not open');await value('document.getElementById("remap-accelerate").click()');
 await send('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'w',code:'KeyW',windowsVirtualKeyCode:87});await send('Input.dispatchKeyEvent',{type:'keyUp',key:'w',code:'KeyW',windowsVirtualKeyCode:87});
 await waitForValue('document.getElementById("remap-accelerate").textContent',v=>v==='W','keyboard remap did not apply');await value('document.getElementById("manual-shifting-toggle").click()');
 const profileSettings=await value('({binding:window.__game.session.save.profile.bindings.accelerate,automatic:window.__game.session.save.profile.assists.automatic,manual:document.getElementById("manual-shifting-toggle").checked})');await value('document.getElementById("settings-back").click()');
 await send('Runtime.evaluate',{expression:`(()=>{window.__pad={id:'QA Virtual Pad',index:0,connected:true,mapping:'standard',timestamp:0,axes:[0,0,0,0],buttons:Array.from({length:16},()=>({pressed:false,touched:false,value:0}))};window.__padConnected=true;Object.defineProperty(navigator,'getGamepads',{configurable:true,value:()=>window.__padConnected?[window.__pad]:[]});return true;})()`});
 const setPad=async(index,pressed)=>send('Runtime.evaluate',{expression:`window.__pad.buttons[${index}]={pressed:${pressed},touched:${pressed},value:${pressed?1:0}};window.__pad.timestamp+=1`});
 await setPad(0,true);await waitForValue('window.__game.mode',v=>v==='playing','gamepad A did not start');await setPad(0,false);await waitForValue('window.__game.input.lastPad.confirm',v=>v===false,'gamepad A release was not observed');
 const padStarted=await send('Runtime.evaluate',{expression:'({mode:window.__game.mode,raceState:window.__game.race.state})',returnByValue:true});
 await send('Runtime.evaluate',{expression:'window.__game.input.autopilot=true;window.__game.race.countdown=.18;true'});
 await setPad(9,true);const pausedValue=await waitForValue('window.__game.mode',v=>v==='paused','gamepad Start did not pause');await setPad(9,false);await waitForValue('window.__game.input.lastPad.start',v=>v===false,'gamepad Start release was not observed');const padPaused={result:{value:pausedValue}};
 await send('Runtime.evaluate',{expression:'window.__padConnected=false'});await waitForValue('window.__game.input.lastPad.start',v=>v===false,'disconnect did not clear pad state');await send('Runtime.evaluate',{expression:'window.__padConnected=true'});await new Promise(r=>setTimeout(r,120));
 await setPad(9,true);const resumedValue=await waitForValue('window.__game.mode',v=>v==='playing','gamepad Start did not resume');await setPad(9,false);await waitForValue('window.__game.input.lastPad.start',v=>v===false,'resume release was not observed');const padResumed={result:{value:resumedValue}};
 await waitForValue('window.__game.car.progress',v=>v>=30,'autopilot did not make progress');await new Promise(r=>setTimeout(r,300));await send('Runtime.evaluate',{expression:'window.__game.writeQA()'});
 const result=await send('Runtime.evaluate',{expression:'JSON.parse(document.getElementById("qa-status").textContent)',returnByValue:true});const qa=result.result.value;
 const shot=await send('Page.captureScreenshot',{format:'png',fromSurface:true});await writeFile(new URL('../artifacts/smoke.png',import.meta.url),Buffer.from(shot.data,'base64'));
 const metrics=await send('Performance.getMetrics'),metricMap=Object.fromEntries(metrics.metrics.map(item=>[item.name,item.value])),heapMB=Number(((metricMap.JSHeapUsedSize||0)/1048576).toFixed(2));
 await send('Emulation.setDeviceMetricsOverride',{width:900,height:900,deviceScaleFactor:1.5,mobile:false});await new Promise(r=>setTimeout(r,700));
 const resized=await send('Runtime.evaluate',{expression:'({canvas:[document.getElementById("game-canvas").width,document.getElementById("game-canvas").height],errors:window.__RALLY_QA__?.errors||[]})',returnByValue:true});
 await send('Runtime.evaluate',{expression:'(()=>{const q=document.getElementById("quality-setting");q.value="low";q.dispatchEvent(new Event("input",{bubbles:true}));return true;})()'});await new Promise(r=>setTimeout(r,500));
 const lowQuality=await send('Runtime.evaluate',{expression:'({canvas:[document.getElementById("game-canvas").width,document.getElementById("game-canvas").height],quality:window.__game.renderer.quality,errors:window.__RALLY_QA__?.errors||[]})',returnByValue:true});
 await send('Input.dispatchKeyEvent',{type:'rawKeyDown',key:'r',code:'KeyR',windowsVirtualKeyCode:82});await send('Input.dispatchKeyEvent',{type:'keyUp',key:'r',code:'KeyR',windowsVirtualKeyCode:82});await new Promise(r=>setTimeout(r,650));
 const restarted=await send('Runtime.evaluate',{expression:'({progress:window.__game.car.progress,countdown:window.__game.race.countdown,state:window.__game.race.state})',returnByValue:true});
 const runtimeNetwork=networkRequests.filter(url=>/^https?:/i.test(url));
 const failures=[];if(corruptSave.result.value.mode!=='title'||corruptSave.result.value.version!==1||corruptSave.result.value.championship!==null||corruptSave.result.value.errors.length)failures.push(`corrupt save recovery failed: ${JSON.stringify(corruptSave.result.value)}`);if(!shell.result.value.titleVisible||shell.result.value.mode!=='title')failures.push('title screen was not the initial usable state');if(!/START STAGE/.test(shell.result.value.startText))failures.push('start action was not discoverable');if(titleLayout.result.value.scrollHeight>titleLayout.result.value.clientHeight+1||titleLayout.result.value.footerBottom>titleLayout.result.value.viewport+1)failures.push(`desktop title shell overflowed: ${JSON.stringify(titleLayout.result.value)}`);if(selectedPractice.stage!=='aurora-forest'||selectedPractice.car!=='lumen-f2'||selectedPractice.world!=='aurora-forest')failures.push(`practice selection was not applied: ${JSON.stringify(selectedPractice)}`);if(!resumeVisible||firstRunId!==resumedRunId)failures.push(`championship resume changed the live run: ${firstRunId}/${resumedRunId}`);if(firstResult.eventIndex!==1||firstResult.rows!==6)failures.push(`first result classification was incomplete: ${JSON.stringify(firstResult)}`);if(secondSelection.stage!=='aurora-forest'||secondSelection.world!=='aurora-forest')failures.push(`second championship content was not selected: ${JSON.stringify(secondSelection)}`);if(championshipFlow.phase!=='classified'||championshipFlow.results!==2||championshipFlow.rows!==6)failures.push(`championship classification was incomplete: ${JSON.stringify(championshipFlow)}`);if(firstResult.audioVoices>10||championshipFlow.audioVoices>firstResult.audioVoices)failures.push(`audio voices grew across championship: ${firstResult.audioVoices}/${championshipFlow.audioVoices}`);if(profileSettings.binding!=='KeyW'||profileSettings.automatic!==false||profileSettings.manual!==true)failures.push(`profile settings did not persist: ${JSON.stringify(profileSettings)}`);if(padStarted.result.value.mode!=='playing')failures.push(`gamepad A did not start: ${JSON.stringify(padStarted.result.value)}`);if(padPaused.result.value!=='paused'||padResumed.result.value!=='playing')failures.push(`gamepad Start pause/resume failed: ${padPaused.result.value}/${padResumed.result.value}`);if(!qa.booted)failures.push('game did not boot');if(qa.errors?.length)failures.push(`runtime errors: ${qa.errors.join('; ')}`);if(!Number.isInteger(qa.audioVoices)||!Number.isInteger(qa.recoveries)||!Number.isInteger(qa.contacts))failures.push(`QA counters missing: ${JSON.stringify({audioVoices:qa.audioVoices,recoveries:qa.recoveries,contacts:qa.contacts})}`);if(qa.frameCount<40)failures.push(`only ${qa.frameCount} frames`);if(qa.progress<25)failures.push(`autopilot progress ${qa.progress}m`);if(qa.cpuFrameMs>16.7)failures.push(`CPU frame ${qa.cpuFrameMs}ms`);if(qa.drawCalls>40)failures.push(`draw calls ${qa.drawCalls}`);if(qa.triangles>50000)failures.push(`triangles ${qa.triangles}`);if(resized.result.value.canvas[0]<1200||resized.result.value.canvas[1]<1200||resized.result.value.errors.length)failures.push(`high-DPI resize failed: ${JSON.stringify(resized.result.value)}`);if(lowQuality.result.value.quality!=='low'||lowQuality.result.value.canvas[0]>=resized.result.value.canvas[0]||lowQuality.result.value.errors.length)failures.push(`low-quality fallback failed: ${JSON.stringify(lowQuality.result.value)}`);if(heapMB>256)failures.push(`heap use ${heapMB} MB`);if(runtimeNetwork.length)failures.push(`runtime network requests escaped the local build: ${JSON.stringify(runtimeNetwork)}`);if(restarted.result.value.progress>25||restarted.result.value.countdown>1)failures.push(`keyboard restart was not immediate: ${JSON.stringify(restarted.result.value)}`);
 console.log(JSON.stringify({corruptSave:corruptSave.result.value,shell:shell.result.value,titleLayout:titleLayout.result.value,practice:selectedPractice,championship:{resumeVisible,firstRunId,resumedRunId,firstResult,secondSelection,...championshipFlow},profileSettings,gamepad:{started:padStarted.result.value,paused:padPaused.result.value,resumed:padResumed.result.value,disconnectReconnect:true},gameplay:{...qa,heapMB},offline:{runtimeNetworkRequests:runtimeNetwork},resized:resized.result.value,lowQuality:lowQuality.result.value,restarted:restarted.result.value},null,2));if(failures.length)throw new Error(failures.join(', '));console.log('Browser smoke test passed; screenshots: artifacts/title.png, artifacts/smoke.png');
 await send('Page.close');ws.close();
}finally{
 try{ws?.close();}catch{}
 try{if(process.platform==='win32')child.kill('SIGKILL');else process.kill(-child.pid,'SIGKILL');}catch{try{child.kill('SIGKILL');}catch{}}
 await rm(profile,{recursive:true,force:true});
}
