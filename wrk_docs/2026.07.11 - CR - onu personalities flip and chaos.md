# Onu personalities, Flip, and Chaos — code review

**Status:** APPLIED AND VERIFIED
**Date:** 2026-07-11

One independent implementation review examined the committed diff against the
signed-off HLD. Codex triaged and applied every concrete finding. Because the
review found one blocker, the same reviewer ran the single permitted targeted
re-review over only the affected seams.

## Findings and disposition

1. **Blocker — a jumping Chaos 7 could not resolve its swap target.**
   Confirmed. The pending-command guard accepted `play` but rejected the
   resubmitted `jump`. The guard now accepts both, and a three-command regression
   reproduces the original deadlock path. Targeted re-review: **VERIFIED FIXED**.
2. **Issue — stacking from two cards bypassed Onu declaration and catch rules.**
   Confirmed. Stack commands now apply the same declaration bookkeeping as normal
   plays. Missed declarations queue until the penalty chain resolves; declared
   and undeclared branches have direct regressions. Targeted re-review:
   **VERIFIED FIXED**.
3. **Issue — an AI Jump-In delay could continue behind new-match UI.**
   Confirmed. AI delays now use the shared reaction scope, which pauses when
   hidden, disables conflicting controls, and cancels with the session. The mobile
   browser drive proves new-match cannot open during the window. Targeted
   re-review: **VERIFIED FIXED**.
4. **Issue — the browser drive missed signed-off controls.**
   Confirmed. Deterministic fixtures now activate draw, hand, Onu, colour, target,
   Catch, and Jump-In paths at both phone sizes. The drive also cancels during draw
   sequencing and checks the AI reaction lock.

## Quality-only cleanup

- Removed the unused `pendingChallenge` copy; `pendingStack` already holds the
  complete latest challenge obligation.
- Kept the browser as one small controller module and the rules in the pure
  command engine. No framework, dependency, feature flag, or generic extension
  layer was added.
- Made a departing Officer Reggie panel non-interactive during its exit animation,
  so it cannot cover a live reaction control.

No review finding remains open.
