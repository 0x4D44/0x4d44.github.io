# OxAlphaRally — AAA build plan

Date: 2026.08.22 · Owner: lead session (goal `OxAlphaRally`) · Status: active

## Mission

A new rally game in `/Users/md/language/0x4d44.github.io/OxAlphaRally/` at the level of
the best modern rally games, with the feel of the original Colin McRae Rally era: fast,
scary, readable. Every domain at AAA quality — handling, stages, damage, weather, audio,
pace notes, graphics. Original assets and branding only (no real drivers, manufacturers,
trademarks, copied layouts or sounds). A harsh independent critic gates every slice and
must be *wowed* — blind side-by-side against the best modern games and real rally footage
— before a slice closes.

## Baseline decision

Seeded from `opus-rally/` @ trunk commit `0832ef2` (copy taken 2026.08.22). Rationale:
strongest local foundation — true 4-wheel model (Pacejka-style tyres, per-wheel struts,
centre-diff drivetrain, turbo/anti-lag), Euler-spiral corner-grammar stage generator,
HDR post pipeline (radial blur → chroma → bloom → ACES → grade → vignette → dither),
100% procedural WebAudio engine synthesis (firing-impulse additive stack), TTS co-driver,
damage model (~28 components), touch controls, replay, career; 434 unit assertions plus
a drivability oracle that autopilots all 12 book stages. Its published defect list
(`opus-rally/OUTSTANDING.md`) is adopted verbatim as the opening backlog.
`cairn-run-rally` stays untouched (mid-gauntlet, separate IP); its matrix-oracle idea is
ported later.

## Law of the codebase

`CONTRACTS.md` (copied from opus-rally) remains binding: plain ES modules, SI units,
+Y-up +Z-forward, no hot-path allocation, seeded RNG only (`rng.js`), headless-testable
pure paths, one owner per module with pinned data shapes. Rebrand renames product-facing
identity only (title, visible strings, car/stage/event names as content work touches
them); internal module seams keep their names unless renamed coherently in one pass.
Keep `THREE-LICENSE.txt` beside the vendored three.js bundle forever.

## Opening backlog (= opus-rally OUTSTANDING.md, verify each before fixing)

P0 — player-blocking / critic-cited:
1. Car reads as a slab: closed-hull deck at belt-line kills cockpit view, hides dash and
   wheel, no glasshouse depth, no cabin trim. Rebuild body loft + interior.
2. Restart on a low-grip climb strands the player — auto-clutch must slip from rest.
3. Headlights blow out to pure white; night reads as dusk (moon intensity/exposure).
4. Snow renders as hard white squares.
5. Terrain skin interpenetrates road ribbon up to 0.81 m — conform lattice vertices near
   the centreline (`meshes.js`).

P1 — correctness:
6. `surfaceAt.onRoad` keys off |lateral| alone — grip reported 50 m past the finish.
7. `speedProfile` lacks a vertical-curvature term and models the engine as P/(m·v) —
   crest launches the car at speeds the profile authorised, mud-climb speeds are false.
8. Scenery blows its triangle budget (272k/314k vs declared 240k); nothing thins it.
9. Verify touch controls actually fixed (`touch.js` postdates OUTSTANDING) — else fix.

P2 — polish: FOV/speed-feel audit (vertical-vs-horizontal lesson), camera rigs, particles.

## Workstreams (disjoint file ownership)

| WS | Scope | Owns |
|----|-------|------|
| W-CAR | Body loft, glasshouse, cockpit interior, liveries | `meshes.js` car sections |
| W-WORLD | Terrain-road conform, scenery budget/thinning, sky/weather/lighting, snow, headlight pool | `meshes.js` terrain sections (after W-CAR lands), `render.js`, `weather.js` |
| W-PHYSICS | Rest/clutch slip, autoShift audit, speedProfile torque+curvature, surfaceAt contract fix, feel tuning | `physics.js`, `stage.js` profile fns, `surfaces.js` |
| W-AUDIO | Per-car engine character, surface layers, co-driver timing | `audio.js`, `pacenotes.js` |
| W-SHELL | UI/HUD/career/touch, rebrand pass, menus, phone controls | `ui.js`, `hud.js`, `career.js`, `touch.js`, `index.html` |
| W-STAGE | New regions/stages via corner grammar; landmarks; pace-note rhythm | stage book data, new content files |

Sequencing rule: `meshes.js` is single-owner at any instant — W-CAR first, then explicit
handoff to W-WORLD for terrain conform.

## Critic protocol (the gate)

After each slice the lead runs evidence capture, then spawns an independent harsh-critic
sub-agent (fresh context, briefed to be brutal):

- Evidence via `tests/shoot.mjs` at pinned quality (the autoscaler would photograph
  SwiftShader at lowest preset); manifest.json proves car state — **a frame whose surface
  reads GRASS is not evidence**. Cockpit AND chase cams; day, night, wet, snow states.
- Critic judges against a written AAA rubric (Colin McRae Rally-era feel plus modern
  rally-game presentation), ranks our frames blind against reference descriptions of
  best-in-class footage where feasible, and returns a verdict per lens: visuals /
  handling telemetry / audio / stage character.
- Verdict scale: WOWED or NOT-THERE with specific defects. NOT-THERE loops the owning
  worker with the defect list. No slice closes on anything less than WOWED.
- Gates always green first: `npm test`, `npm run validate`, browser gate.

## Methodology lessons carried forward (opus-rally "things that turned out to be lies")

1. Measure output pixels, not input radiance — mirror exposure/ACES/sRGB in weather
   assertions so tests measure what the eye sees.
2. Never anchor source scans on newline sequences — CRLF checkouts silently blind them.
3. A browser gate must actually START a stage, not just generate one (11 of 12 could not
   start once).
4. Correct units composed wrong still break — camera fov is vertical; author it as such.

## Progress log

- `92e2003` — baseline seeded from opus-rally @ trunk `0832ef2`; suite 509/509 green
  (incl. drivability oracle + browser gate).
- `ebeb8fa` — W-CAR unit 1 landed, 513/513: registered glass panes (damage-aware),
  steering wheel in its own rim frame ready for game.js rotation, column separated,
  interior under beltline; red-first deck/cockpit-ray/glass tests.
- Eyes-on `shots/hairpin.png` (lead): world still NOT-THERE — flat lighting, no shadows,
  washed sky, toy-like exterior body, speckle gravel. Confirms W-WORLD and a W-CAR body/
  livery unit are the highest-value next slices.
- W-PHYSICS: worker attempt 1 stalled leaving 12 red (archived, reverted to green);
  attempt 2 no-op. Next: implement clutch-from-rest + launch matrix as ONE narrow unit,
  then speedProfile (torque/curvature) + surfaceAt bounds as a second unit.
- Ops notes: sub-agent task results came back empty twice while work was real — always
  verify via `git status`/tests, never via the report. `shots/` at worktree root is
  regenerable scratch (do not commit). Stalled attempt diff: `$TMPDIR/oxar-wphysics-attempt1.patch`.

## Critic log

### Pass 1 — lead eyes-on, 2026.08.22, fresh captures @ pinned medium 1280x720

Frames judged: golden, cockpit, night, snow (+ hairpin from the prior set). Independent
critic sub-agent was attempted twice and returned empty reports both times (known pipeline
flake); the lead ran the harsh pass directly. Retry the independent critic next pass.

Calibration rankings (within viewed set): golden > night (more material info);
snow > cockpit (depth beats the dark cabin); golden > snow; hairpin > cockpit.

Per-lens verdicts:
- VISUALS: **NOT-THERE** vs EA WRC, though structurally much improved — night is genuinely
  dark with a readable headlight pool, snowflakes are soft dots (no more hard squares),
  fog depth layers work, ruts/crown read on the gravel, wires/poles give speed. Gaps: sky
  is a flat wash with no sun disc/cloud structure or grade warmth; ground is repeating
  speckle tiling with no near-road detail; hard aliasing on road edges; distant terrain
  blobs lack aerial perspective.
- CAR: **NOT-THERE** — proportions/glasshouse/cage now real (W-CAR unit 1 visible), but the
  body is flat-shaded with no key-light response, livery reads as a stuck-on stripe, and
  in cockpit the steering wheel is a black void with no binnacle read; glass has zero
  presence (no tint/reflection).
- STAGE CHARACTER: **CLOSE** — co-driver calls are authentic ("3 LEFT + BRAKE + INTO
  CREST, THEN 6 RIGHT"), corner signs/crests/compressions give rhythm; but the snow stage
  dresses nothing (green verge/trees under a snow preset — weather-only, no world snow).
- HUD/UI: **CLOSE/WOWED-** — clean, authentic, readable; damage panel + traces + rev
  counter all land. Menu not judged this pass.
- HANDLING-EVIDENCE: mechanics credible (oracle green, hill-start + launch-ranking
  assertions), but fun is unproven until a driver plays it — needs the play-test pass.

Top-5 highest-leverage fixes (order):
1. Car material/light response + lit cockpit (wheel rim, dials, glass presence) —
   meshes.js materials, render.js lights. The car is in every frame.
2. Sky structure + per-preset grade: sun disc/glow, cloud shapes, warm/cool grade —
   weather.js/render.js. The empty sky flattens every frame.
3. Ground macro-variation: break speckle tiling, add verge dressing (stones, tufts,
   edge scatter) — meshes.js terrain.
4. Snow stage world-dressing: verge/tree snow blankets, ice patches, plough banks —
   content + meshes kits.
5. Edge quality (MSAA/AA at medium) + headlight beam shape (twin beam, throw ahead) —
   render.js.

### Pass 2 — after fixes #1/#2/#5, 2026.08.22 (lead eyes-on; independent critic still returning empty)

- Fix #2+#5 landed (`ac0617c`, worker W2, full report): sky sun glow + cloud deck +
  warm/cool grade; FXAA-style AA; headlight beam reshaped (twin structure, hotspot 13-20 m
  ahead, wide spill). Verified 158 weather+render green, validate-static 86, and eyes-on:
  night now throws light down the road (CLOSE+), golden reads warm with cloud structure
  (CLOSE). Remaining night gap: beam fan edges read synthetic; no rim light on passing trees.
- Fix #1 unit A landed (`a2a61dd`, worker W1 retry, report empty — verified on disk +
  178 meshes/render green + eyes-on): wheel rim/spokes lit, binnacle + emissive dials,
  glass tint, cabin fill. Cockpit improved but STILL the weakest view: wheel barely lit,
  dials not readable from the eye camera, cabin sparse (no seats/harness read). Cockpit
  iteration 2 queued.
- Still open from pass 1: #3 ground macro-variation/verge dressing, #4 snow world-dressing
  (both meshes.js — next owner slot), cockpit iteration 2, body material response (unit B,
  not attempted), independent-critic retry.

### Pass 3 checkpoint — harness bug found and fixed, 2026.08.22

- W4 (`a801003`, full report): beam cut-off softened, scenery rim term (signs
  retroreflect at night — verified eyes-on in shots5/night.png), speed streak spends by
  ~180 km/h, medium enables radial blur. 164 render+weather green, red-first snippets kept.
- **Harness bug (W4 find, lead-fixed)**: shoot.mjs + browser.test.mjs navigated
  `/opus-rally/` — the stale committed copy — so critic pass 1's `shots/` frames and every
  prior browser-gate run validated the OLD game. Both now derive APP_PATH from their own
  location; browser gate green against the real tree; shots5/ is the first trustworthy
  capture set. Critic pass 1's code-level conclusions stand (fixes are test-pinned), but
  its frame evidence is superseded — re-judge everything from shots5/ onward.
- W3 (ground detail + snow dressing) no-oped this round — still open, meshes.js owner slot
  free. Also open: cockpit iteration 2, body material response, independent-critic retry
  on shots5/ or newer.

### Pass 3 — independent critic on shots5/ (trustworthy captures), 2026.08.22

First full independent-critic report (pipeline finally held). Blind rankings given;
per-lens: VISUALS NOT-THERE (ground speckle shimmer #1, mottled clouds, blob shadows),
CAR NOT-THERE (toy proportions, arch gaps, blank plate, no driver), STAGE CHARACTER CLOSE,
HUD-UI **WOWED** (first top mark — pacenote stack + tacho + traces), HANDLING-EVIDENCE
CLOSE (deepest evidence base the critic had seen in this format; 61/61 oracle green).
Overall: NOT-THERE vs EA WRC, CLOSE as a stylised rally game; "the gap is concentrated:
three materials (ground, car paint/wheels, cabin), one missing world-state (snow), and
proof-of-air."

Fixes landed in direct response (same round): ground macro-zones + verge dressing + snow
world-state (`7de9863`, W3, verified eyes-on: snow.png now winter) — note the critic's
frames predate this commit, so its #1/#2 need re-judging rather than re-implementing; the
base-frequency shimmer may persist under the new macro blend.

Queue for pass 4: cockpit legibility (emissive dials readable from eye camera, cabin fill
matched to exterior exposure, wheel specular), car model pass (wheels fill arches, stance,
livery decals, driver silhouette), ground shimmer de-speckle at distance, jump proof
(apex frame with detached shadow + landing squat) and render the skidmarks the sim already
tracks, then fresh captures and critic pass 4.

### Pass 4 checkpoint — attempts archived, green restored (2026.08.26)

Pass-4 work moved to a fresh worktree after pass 1-3 integrated to main (`a6fad6f`).
Two workers (car model + ground de-speckle; cockpit legibility + jump proof + skidmarks)
wrote their red-first tests (~500 lines) but both stalled mid-implementation, leaving 7
red. The lead archived both diffs and restored the committed green state (537/537):

- `$TMPDIR/oxar-pass4-meshes.patch` — car model unit (wheel/arch fill, stance, door/roof
  number panels, driver silhouette) + ground distance de-speckle; includes its failing
  tests, which are the spec.
- `$TMPDIR/oxar-pass4-render.patch` — cockpit legibility (exposure-matched cabin fill,
  dial contrast, wheel specular), jump-proof capture (airborne manifest evidence),
  skidmark rendering; includes its failing tests.

Resume path: `git apply` a patch in this worktree, run its tests to see the red list,
finish the implementation unit by unit (each patch is one workstream), verify, commit.
Still queued behind these: almanac catalog wiring (data.js entry, tags, shelf, back-pill
include) so the game appears on the site; then critic pass 4 on fresh captures.

## Definition of done

- Every workstream WOWED by its critic; full defect backlog closed with red-then-green
  regression tests.
- `npm test` + `npm run validate` + browser gate green in `OxAlphaRally/`; repo-root
  responsive test green (no sideways scroll at 390/768; nothing under the back pill).
- Almanac integration: `data.js` ESSAYS entry + tags from `TAG_GROUPS`, a COLLECTIONS
  shelf, `<script defer src="/almanac-back.js"></script>` before `</body>`.
- Original branding throughout; no push to `origin/main` without Arthur's say-so.
