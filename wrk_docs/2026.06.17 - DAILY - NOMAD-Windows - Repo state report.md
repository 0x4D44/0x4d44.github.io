# Daily report — 0x4d44.github.io

- **Date:** 2026-06-17
- **Host / env:** NOMAD / Windows
- **Working tip observed:** local `main` = `8ea1a3e`; `origin/main` = `185c47d` (local 1 ahead, unpushed)
- **Pass type:** read-only observation. Method: 3 parallel readers (commit log / catalog / hygiene) + adversarial verification (2-of-3 skeptics per significant claim; 6 of 16 claims rejected and dropped).
- **Baseline:** this is the **first** daily report — `wrk_docs/` did not exist before this pass, so there is no prior report to trend against. Trend statements below are first-observations, to be compared from the next pass on.

---

## Headline

The catalog is in **excellent health — zero defects across all integrity checks** — sitting on top of a **bursty week** of heavy authoring (40 commits in 7 days; the document corpus grew ~47 → 55, about +17%) that has now tailed off into light housekeeping. The one real, actionable risk is **repo hygiene**: there is **no `.gitignore`** on a deploy-by-push-to-Pages repo that also forbids publishing certain files, and a **23 MB untracked `.impl-work/` scratch pile** (plus a stray mis-named PNG) sits loose in the root. Finally, this pass ran **while a concurrent maintenance session was live-editing the tree** (uncommitted `README.md` + `CLAUDE.md`, an unpushed journal commit, a transient `bugs/` dir) — observed read-only, untouched.

---

## 1. Bug ledger — direction of travel

**No `bugs/` ledger exists** in the committed tree. There is no defect-tracking apparatus (`bugs/`, `QUALITY-GUIDE.md`, `TESTING-GUIDE.md` are all absent). Open/Fixed/Closed counts are therefore **0 / 0 / 0 — n/a**.

> A `bugs/` directory appeared and vanished twice during this pass (transient scratch from the concurrent session / tooling, never populated with bug files). It is **not** a real ledger; do not treat the flicker as a tracked-bug signal. *Confirmed: an early reader hallucinated a "`bugs/README.md` with an `ALM-*` grammar"; adversarial verification refuted it 3/3 as fabricated — no such file exists.*

For a single-author static almanac this absence is reasonable; defects here surface as commit-message fixes (see §3) rather than ledger entries. **Trend:** none to report (first pass).

## 2. Design backlog — complete / in flight / not started

There is **no design or backlog apparatus** in the repo — no `wrk_docs/` (until this report), no PLN/HLD docs, no roadmap file. Direction is inferred from the commit log and the live working tree.

- **Complete & shipped (recent):** The Second World War atlas, The Night Cab, The Peopling of the Earth (world-population), Worldviewer, The Cab (emu-cab), and the isochrone family (mdisochrone / Edinburgh / UK) — all `real: true` and live in the catalog.
- **In flight (uncommitted, by a concurrent session — see §6):** a human-facing `README.md`; a +9-line `CLAUDE.md` doc edit (tag vocabulary + a `wrk_journals` note); an unpushed reconciliation journal commit (`8ea1a3e`).
- **Designed-but-not-started:** nothing tracked. No backlog to draw from.

## 3. Commit history & cadence

- **Scale:** 79 commits, 2026-05-26 (`896b2c7` "Initial user-site hub") → 2026-06-17 (`8ea1a3e`).
- **Recent volume:** last 7 days = **40 commits** (~half of all commits ever); last 3 days = 20.
- **Per-day cadence (active window):** 06-12: 3, 06-13: 6, **06-14: 14 (peak)**, **06-15: 12**, 06-16: 1, 06-17: 3. Shape is **bursty** — a two-day 14+12 surge then a sharp drop into light catalog/chore/journal commits. (Not a steady acceleration; and as the first report there is no prior cadence to call this an anomaly.)
- **Corpus vs churn — important caveat:** the 40-commit week is *not* a doubling of content. The catalog went from ~47 entries (06-10 state) to **55** now — **+8 documents, ~17%**. The bulk of the churn was iterative polish/rebuilds on a few documents, not new corpus. *(An early "corpus nearly doubled" claim was refuted 3/3 — commit churn ≠ corpus growth.)*
- **Most-active projects (recent), largest first:**
  - **Isochrone family** (`mdisochrone` / `edinburgh-isochrone` / `uk-isochrone`) — the largest cluster, ~15 commits 06-12 → 06-14 (new chart, time-travel eras, fix sweeps, then UK/Edinburgh/Aberdeen/Dundee detail).
  - **The Night Cab** — 5 commits 06-15 → 06-16 (add, rainy default, daylight, look-around, Grand World rewire); the only project touched on 06-16.
  - **WW2 atlas** — 5 commits all 06-15 (add + four refinement/critical-review passes).
  - **world-population** — 4 commits 06-14 → 06-15; **worldviewer** — 2 redeploy builds.
- **Iteration pattern:** add → rapid iterative refinement, including explicit *"review fixes" / "adversarial-review" / "critical-review"* passes within hours-to-a-day of the add (mdisochrone, Edinburgh, WW2 atlas). *Verified as a real pattern, but with a nuance: it is a **recent habit** concentrated in the 06-12 → 06-15 cluster, not the repo-wide historical norm (earlier history was bulk-batch "Add N documents" commits with little per-doc follow-up).*
- **Fix vs feature:** the week skews toward new docs / feature iteration over regression fixes; the genuine fixes (~9 over the window) are mostly in the isochrone family, plus one-offs (`accf5de` mercury-arc startup crash, `ca397e7` emu-cab reverse-creep, `a74cda7` worldviewer bug-fix sweep). *(Precise per-commit fix/feature ratios from the first reader were refuted as miscounted; the directional read holds, the exact numbers do not.)*
- **Versioning:** only **The Night Cab** surfaces semver in commit subjects (`v0.8.0`, `v0.10.0`) — and those are its upstream `mdtrain2` versions carried in import messages, not a repo-level discipline. The root is a buildless static catalog with no version manifests, so "code change → version bump" does not apply at root. *(A "Night Cab versioning discipline" framing was refuted as trivial.)*

## 4. Open branches / worktrees

- **Branches:** only `main` (local + `origin/main`). No long-lived feature branches.
- **Worktrees:** the sacred main clone `C:/language/0x4d44.github.io` + one **ephemeral** worktree created by *this* pass (`C:/worktrees/0x4d44.github.io/daily-2026-06-17`, branch `task/daily-2026-06-17`) to land this report; it will be removed after integration.
- **Divergence:** local `main` (`8ea1a3e`) is **1 commit ahead of `origin/main` (`185c47d`)** — the sibling session's reconciliation-journal commit is committed locally but **not yet pushed**.

## 5. Catalog & document inventory — **GREEN, 0 defects**

- **55 `window.ESSAYS` entries, all `real: true` (0 drafts).**
- **Directory mapping:** every entry's URL path maps to an existing `<slug>/` directory and every one of the 55 document directories has an entry — **no broken links, no orphan dirs**. The only 3 slug↔URL divergences are the documented intentional crossings (`east-coast-main-line`→`/ecml/`, `ecml`→`/ecml-timeline/`, `ai-watched`→`/the-ai-who-watched/`).
- **Illustrations:** all 52 distinct `illustration` ids referenced in `data.js` have a defining `<symbol id="ill-…">` in `index.html`; no missing id (so no silent `ill-diesel` fallback) and no orphaned symbol.
- **Tags:** the 11 distinct entry tags are all valid `window.TAGS` chips and every chip (bar `all`) is used. Distribution: software 36, engineering 16, history 15, rail 11, transport 5, science 4, fiction 2, math 2, comic 1, finance 1, maritime 1.
- **Newest 5 (by `date`):** the-second-world-war (06-15 18:00), night-cab (06-15 10:00), world-population (06-14 16:30), worldviewer (06-14 15:30), emu-cab (06-14 15:00).

## 6. Repo hygiene & working state

- **No `.gitignore`** anywhere (confirmed: `git check-ignore` returns "not ignored" for everything untracked). On a deploy-by-push-to-`main` Pages repo that *hard-forbids publishing Praedex files*, this makes the untracked clutter a real **`git add -A` publish foot-gun** — the only guard today is the global "stage explicit paths, never `git add -A`" discipline.
- **`.impl-work/` — 80 files / ~23 MB** of pure scratch (67 screenshots, 7 JS build scripts, 4 JSON dumps) from the 14–15 June WW2-atlas / world-population / night-cab sessions. Never referenced by the catalog. The kind of working state that belongs in `scratchpad.md`/`wrk_*`, not loose in the root.
- **Stray `.impl-work$1.png`** at the root — a 305 KB PNG, byte-identical (`md5 340aa4be…`) to `.impl-work/a_about.png`; its name is an **unexpanded shell positional (`$1`)** from a botched redirect. It is **untracked scratch debris** (never committed, never deployed) — *not* a deployed defect. Clean-up candidate, but it is uncommitted in-flight tree state, so coordinate before deleting (see standing rule on others' uncommitted work).
- **Praedex sweep: CLEAN** — no `Praedex*.zip`, `*Deep Dive*.html`, or `Praedex UI.zip` anywhere (tracked or untracked). The 33 tracked `.zip`/`.pdf` are legitimate per-document archives.
- **`.deltic-toast.png`** at root looks like clutter but is **tracked & clean** (committed `f504359`) — not a hygiene issue.
- **Concurrent session — observed, untouched (read-only):**
  - Untracked **`README.md`** — a polished, complete human-facing project README ("55 documents"). Ship-worthy in-flight work; should land.
  - **`CLAUDE.md`** carries a real **+9-line uncommitted edit** (tag-vocabulary list + a `wrk_journals` note).
  - The repo state visibly churned during this pass (HEAD advanced 3 commits; `bugs/` flickered in and out; `CLAUDE.md` re-dirtied). The report is an explicit point-in-time snapshot.

---

## Tracked issues (living list — carry forward, ~key items)

| ID | Sev | Item | State | Evidence / notes |
|----|-----|------|-------|------------------|
| HYG-1 | **med** | No `.gitignore` on a deploy-by-push Pages repo → `git add -A` publish foot-gun | **open** | `git check-ignore` = not-ignored for all untracked; CLAUDE.md "never `git add -A`" is the only guard |
| HYG-2 | low | `.impl-work/` — 23 MB / 80 files of scratch loose in root | **open** | untracked; from 14–15 Jun sessions; relocate or clear (coordinate — in-flight) |
| HYG-3 | low | Stray `.impl-work$1.png` (botched `$1` redirect, dup of `a_about.png`) | **open** | untracked debris; delete with HYG-2, not standalone |
| DOC-1 | low | `README.md` authored but **uncommitted** (concurrent session) | **in flight** | complete, ship-worthy; should be committed |
| DOC-2 | low | `CLAUDE.md` +9-line edit **uncommitted** (concurrent session) | **in flight** | tag vocabulary + `wrk_journals` note |
| GIT-1 | low | `origin/main` (`185c47d`) lags local `main` (`8ea1a3e`) by 1 unpushed commit | **open** | sibling reconciliation journal; integrate or let sibling push |
| INFRA-1 | info | No bug ledger / quality / testing apparatus (`bugs/`, `QUALITY-GUIDE`, `TESTING-GUIDE`) | **n/a** | reasonable for a solo static almanac; noted for the report machinery |
| CAT-1 | — | Catalog integrity: **GREEN** (55/55 dirs, 52/52 symbols, 11/11 tags, 0 drafts) | **green** | track the clean state; re-verify each pass |
| VER-1 | info | No root versioning (only night-cab carries upstream semver in imports) | **n/a** | buildless static catalog; "code change → bump" N/A at root |

---

## Conclusions & proposed plan for today

The repo is **healthy where it counts** (catalog integrity green, no broken links/icons/tags, no drafts, no defect backlog) and is coming off a productive authoring burst. The only material risk is **hygiene/publish-safety**, and there is **active concurrent work** to coordinate around. Proposed actions for the *action-the-daily* (B) pass — **none actioned here; this pass is read-only**:

1. **HYG-1 (highest value): add a `.gitignore`** covering `.impl-work/`, `*$1.png`, and the common scratch patterns, to turn the "never `git add -A`" rule from remembered into enforced. Smallest correct fix for the biggest standing risk.
2. **HYG-2 / HYG-3: clear or relocate `.impl-work/` (23 MB) and remove the stray `.impl-work$1.png`** — but only after confirming it is not the concurrent session's live scratch (it is untracked in-flight tree state; coordinate first per the multi-agent rule).
3. **DOC-1 / DOC-2: let the concurrent session land its own `README.md` and `CLAUDE.md` edits** (do not grab another agent's uncommitted work). If it has clearly finished and abandoned them, commit on their explicit paths.
4. **GIT-1: integrate `origin/main`** — the unpushed `8ea1a3e` should reach the remote (this report's own push will fast-forward it along; otherwise the sibling pushes it).
5. **Catalog: no action** — green; just keep the per-pass integrity check as a regression guard.

**Headline trend (first observation):** heavy-authoring burst → cooling into housekeeping; catalog integrity green; hygiene/publish-safety is the one open risk to close.
