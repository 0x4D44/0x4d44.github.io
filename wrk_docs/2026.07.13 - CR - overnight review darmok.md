# 2026.07.13 — CR — Overnight code-review pass

One coverage-ledger-driven review pass per the overnight `/loop` review prompt.
Read-only: reviews code, files tracked bugs, files Draft requirements. Does **not**
fix, run the app, or run the exploratory-test harness.

Area picked this pass: **`darmok/`** — the largest never-reviewed body of
hand-written logic (`engine.js` 500 + `app.js` 1232 + `kanji.js` 411 lines), with
the thinnest test coverage (one 66-line data-only test; the 500-line learning engine
and the 1200-line controller are unguarded) and active churn (HLD landed 2026-07-11).

Method: `/deep-review` hand-rolled fan-out — 7 lenses (correctness, security,
reliability, performance, maintainability, test-coverage, devil's advocate) reading
the explicit file slice in parallel, each finding adversarially verified by a skeptic.
The three highest-impact engine defects were additionally reproduced by me directly
against the shipped `engine.js` (deterministic repros, not a vote). Findings below
are **confirmed only**; refuted ones are listed at the end with the reason.

---

## darmok/ — confirmed findings (bugs filed)

Severity is the skeptic-corrected value. All are user-visible unless noted.

| Sev | Finding | file:line | Bug | Effort |
| --- | --- | --- | --- | --- |
| **Med** | Answer grading rejects the rōmaji the app itself teaches (long-vowel ー / apostrophe / づ-ず): ~20/513 words marked wrong when you type the card's own rōmaji; a wrong-grade also demotes SRS. Same fold also *false-accepts* かれー≡かれ, すきー≡すき. | `engine.js:103-110` | KILN-00006 | S |
| **Med** | Kanji breakdown modal ✕ close button is dead — `data-act="close-kanji"` has no handler and no Escape; only a thin backdrop tap dismisses (reads as hung on mobile). | `app.js:770` | KILN-00007 | S |
| **Med** | Match-exercise retry is unwinnable — `_matchMistakes` not reset in the requeue copy, so a flawless retry still grades wrong; affects all 6 match exercises. | `app.js:585` | KILN-00008 | S |
| **Med** | Authored exercises are shared `DK.CURRICULUM` objects — transient play-state (`_placed`/`_hidden`/`_assisted`) leaks into replays, auto-solving build cards and permanently depressing replay scores. | `engine.js:357` | KILN-00009 | S |
| **Med** | ASK DATA / the A→あ IME toggle wipe the answer the user is typing (input has no `value`, `rerenderEx()` rebuilds it empty) — the two buttons a struggling learner most needs. | `app.js:510,1204` | KILN-00010 | S |
| **Med** | SRS keys embed the mutable English gloss; orphaned keys are never pruned, so a content edit inflates the DRILL badge and can make BEGIN DRILL a dead click. | `engine.js:260,251,386` | KILN-00011 | M |
| **Med** | Daily spaced-review drills never count as a Training day (`P.days` only written on lesson finish) — the 30-day Long Tour medal is unreachable via the activity the app promotes daily. | `app.js:634` | KILN-00012 | S |
| Low | `DK.load` lets a null container (srs/done/medals) through and interpolates unescaped progress fields → brick on next render + same-origin stored-XSS surface (filed high by the security lens, corrected to low: static site, no auth/secret). | `engine.js:195` | KILN-00013 | M |
| Low | Two vocab entries mis-split furigana (お茶[おちゃ], 問[と]題[だい]) → wrong ruby + a self-contradicting typeback hint. | `weeks01-03.js:45`, `weeks10-12.js:587` | KILN-00014 | S |
| Low | 金曜日 taught with rōmaji `kinyoubi` → converts to きにょうび (should be `kin'youbi`). | `weeks07-09.js:211` | KILN-00015 | S |
| Low | Medals earned during a drill are awarded silently (`checkMedals(null, null)`) — the drill-only "Universal Translator" medal is never celebrated. | `app.js:635` | KILN-00016 | M |
| Low | Data's 2nd hint labels the English gloss as a Japanese "reading". | `app.js:419` | KILN-00017 | S |

**Requirement captured (Draft, for Gate-1 triage):** ALM-REQ-KILN-00002 — add an
engine-level corpus-oracle test for darmok (rōmaji round-trip, furigana↔kana,
`normalizeAnswer` injectivity, `DK.load` totality). The engine is pure and DOM-free;
these four cheap properties would each have pinned one of the bugs above at authoring
time. This is a durable testability gap (the curriculum is edited continuously and is
currently unguarded), not a one-off.

### Report-only (not filed — nit / cleanup)
- Three vestigial hooks misrepresent the design to the next editor: `window.APP = {…}
  // for tests` (no test loads app.js), the written-but-unread `data-ime-off`
  attribute (`app.js:510`; real switch is `S.imeOff`), and the never-assigned
  `P.rateBtn` clause (`app.js:955`). Also `S.drill`'s `firstTry`/`wrongTotal` are
  maintained but never read. Low-value cleanup; left in the report per the
  conservative-capture rule.

---

## news/ — confirmed findings (bugs filed; area NOT logged as reviewed)

**Provenance / caveat:** these came from the deep-review workflow's *first* run, which
fell back to the most recent commit (the 100-article `news` drop) because the fresh
worktree had an empty diff. They are real and adversarially confirmed, so they are
filed rather than discarded — but `news/` was **not** a deliberate, scope-vetted pass,
so its coverage-ledger row stays `(never)`. news/ is a strong candidate for the next
pass, which should vet the by-design set properly.

The workflow returned 18 confirmed (several the same defect across lenses) + 7
correctly held as `needs_reverify` (verify cap hit — treated as UNVERIFIED, not
filed) + 2 refuted. Deduplicated to 7 distinct filed defects:

| Sev | Finding | file:line | Bug |
| --- | --- | --- | --- |
| **Med** | `based-on-truth` articles still carry the "this story never happened / any resemblance is coincidental" satire notice + footer, directly contradicting the same commit's About-page copy (100/255 articles). | `news.js:459,229` | KILN-00018 |
| **Med** | Any data-file load failure blanks the whole page — `seq[0..8]` and `pickAds()[0]` are dereferenced unguarded. | `news.js:327` | KILN-00019 |
| **Med** | Malformed percent-encoding in the query string throws an uncaught `URIError` and blanks the page instead of the graceful path. | `news.js:158` | KILN-00020 |
| Low | Search highlighting regex-replaces over already-escaped HTML, corrupting its own `<mark>` tags and entities. | `news.js:525-533` | KILN-00021 |
| Low | "a fictitious oceanographer at the University of Aberdeen" — LLM-guardrail phrasing leaked into 20 shipped article bodies. | `articles.js:5211+` | KILN-00022 |
| Low | Seven articles quote their own byline correspondent as the independent expert source. | `articles.js:3900+` | KILN-00023 |
| Low | Masthead date mixes the local weekday with the UTC calendar date — wrong for an hour a day under BST. | `news.js:163-165` | KILN-00024 |

Held as UNVERIFIED (not filed — skeptic cap, would need re-run in a news pass):
per-term regex compiled in the results loop; entity-name query corruption; unguarded
`seq[0..8]` (dup of KILN-00019); stale `hue`/pull-quote comments; masthead-date test
gap (dup of KILN-00024); factual-accuracy oracle gap for based-on-truth; a mistyped
category silently half-invisible.

Refuted (not reported): "articles.js is render-blocking on all four pages" (the size
range cited was wrong and article.html tolerates it); "balloon-boy piece names a real
family" (renderer already de-identifies).

---

## Coverage ledger

Reconciled additively: added the 27 current top-level areas missing from the ledger
(new documents shipped since it was last written), so it now lists all 100 code areas.
`darmok/` set to `2026-07-13`. `news/` left `(never)` (see caveat above). No existing
rows reordered or re-flowed.

## Next pass should cover (as written after the darmok pass)
Still never-reviewed and highest-priority: **`news/`** (do the deliberate pass —
largest hand-written corpus + logic, bugs already surfaced above to confirm against),
then the other churn-heavy 2026-07 additions — **`onu/`, `tidecall/`, `brilliancy/`,
`shipshape/`, `span-of-control/`, `cruise-line/`** — all `(never)` and all touched on
2026-07-13. Tier order remains NEVER-REVIEWED first, oldest `(never)` by risk.

---

# Pass 2 — `news/` (deliberate scoped review)

Second bounded pass of the day. Area picked from the ledger: **`news/`** — the top
never-reviewed area (largest hand-written logic: `news.js` 627 lines + `ads.js` 239 +
`articles.js` now 355 stories). This is the deliberate pass the darmok pass flagged;
it read `news.js` and `ads.js` in full by hand (not `/deep-review` — that retargets on
an empty diff, per the lesson) and ran corpus oracles to confirm/refute.

**First, the 7 bugs filed last pass from the misfire (`KILN-00018…00024`) were
re-checked against the current `news.js` on `origin/main` — all still valid**
(disclaimer contradiction at 459-460/229-232, `qs()` URIError at 158, search-highlight
over escaped HTML at 525-533, masthead two-clock date at 163-165, and the two content
issues). None were fixed in the interim; no dupes re-filed.

### New confirmed findings

| Sev | Finding | file:line | Bug | Effort |
| --- | --- | --- | --- | --- |
| Low | **Homepage always shows at least one story twice** — the two category feature bands filter the rotated sequence without excluding stories already in the hero/lead/"More"/"Around" blocks. Reproduced deterministically: **48/48** hourly seeds over the 355-article corpus repeat a story. Cosmetic but 100% on the primary page. | `news.js:343,358` | KILN-00026 | S |
| Low | **Unguarded article-field access blanks the whole search/category/About page** — `a.category/headline/standfirst.toLowerCase()` (news.js:499,507-508,511-512) are not null-safe (unlike `esc()`), so one malformed appended article throws and blanks the page. Corpus is currently clean (verified), so this is a **latent** robustness + test-coverage gap that the "no editorial oversight, just append an object" workflow invites. | `news.js:499,511` | KILN-00027 | S |

### Report-only (low — not filed; nit/cleanup per the conservative-capture rule)
- **Related-story cards are built by regex surgery on generated HTML** (`news.js:441`):
  `cardHtml(x,0).replace(/<div class="meta">[^<]*<\/div>/, …)` re-parses its own output
  to swap the meta line. Works only because `agoStamp(0)` = "Just now" has no `<`;
  fragile if the card markup ever changes. Cleaner: pass a mode flag to `cardHtml` so it
  emits the category meta directly.
- **Search results are unbounded and each row emits a full inline `<svg>`** (`news.js:549-555`):
  a broad query or the `#based-on-truth` tag link (~100 matches) renders ~100 inline
  gradient SVGs in one `innerHTML`. Mild; cap/paginate if it's ever felt on mobile.
- **Empty-search still builds the full 355-article haystack then discards it**
  (`news.js:506-517`): the `.map` lowercases every article body before the `.filter`
  drops all rows when `terms.length === 0`. Wasteful; short-circuit when there are no terms.
- **`adHtml`'s external-link handling** (`news.js:258`): the variable is named `target`
  but only ever carries `rel="nofollow"`; external ad links open same-tab (no
  `target="_blank"`), so no `noopener` is needed — a naming nit, not a defect.

### Not reported (checked, refuted)
- Broken ad cross-promo links: all 7 almanac slugs the ads point to (`emu-cab`,
  `night-cab`, `coil`, `vector-gp`, `worldviewer`, `picoem`, `paint-drying`) exist on
  `origin/main`. No bug.
- Duplicate SVG gradient `id` / hash collision blanking art: each SVG has its own
  `<defs>`; a repeated article (finding KILN-00026) yields a duplicate `id` that the
  browser resolves to the first — visually identical, not a defect.

## Coverage ledger (pass 2)
`news/` set to `2026-07-13`. No other rows touched.

## Next pass should cover (as written after the news pass)
Never-reviewed, churn-heavy 2026-07 areas, tie-broken toward higher risk:
**`onu/`** (personality-flip logic, HLD 2026-07-11), then **`tidecall/`**,
**`brilliancy/`**, **`shipshape/`**, **`span-of-control/`**, **`cruise-line/`** — all
`(never)`. Tier order remains NEVER-REVIEWED first.

---

# Pass 3 — `onu/` (deliberate scoped review)

Third bounded pass. Area picked from the ledger: **`onu/`** — a never-reviewed
"Uno"-like game with three modes (Classic / Flip / Chaos), 10 AI "snake"
personalities, and house rules (stacking, 7-swap, 0-rotate, jump-in, draw-to-match).
Slice: `engine.mjs` (686, pure rules + AI), `app.mjs` (692, async UI orchestration),
`index.html` (483), plus its three test files.

**Headline: this is a well-built, well-tested slice with little to fix.** The engine
already carries a runtime invariant checker (`assertState`) and ships 29 passing tests
— unit + property-style seeded full-game simulations for all three modes + a real
headless-browser (CDP) integration test + static/a11y checks.

**Verification I ran (evidence, not assertion):**
- Ran the suite: **29/29 pass** (`engine.test.mjs` + `validate-static.mjs`).
- Extended the seeded sims into a **9000-game fuzz** (3000 each of classic/flip/chaos),
  driving an autonomous policy that resolves *every* request branch
  (chooseColor/Target, playDrawn, penaltyResponse incl. stack/challenge, catch, jump),
  asserting `assertState` after every transition plus physical-card conservation
  (108 classic / 112 flip). Result: **9000/9000 clean** — zero crashes, hangs, or
  invariant/conservation violations. (The engine correctly *rejected* my initially
  malformed stack/jump commands — those were harness bugs, fixed, after which all
  three modes were spotless.) No engine correctness or robustness defect found.

### Confirmed findings

| Sev | Finding | file:line | Bug | Effort |
| --- | --- | --- | --- | --- |
| Low | **Unguarded top-level `localStorage` read blanks the whole game** if storage access throws (blocked cookies / sandboxed iframe). `app.mjs:46` runs at module import before any try/catch, so a `SecurityError` aborts the module and nothing renders. Inconsistent with the repo's own guarded pattern (darmok wraps every storage call). | `onu/app.mjs:46` (also 637) | KILN-00030 | S |

### Report-only (not filed — nit / by-design-ambiguous)
- **`nextSeat` operator-precedence fragility** (`engine.mjs:142`): `state.direction *
  steps % count` parses as `(direction*steps) % count`. It yields the correct seat only
  because `steps ≤ 2 < count = 4` always holds (the `% count` on the product is a no-op
  for these inputs). Verified not a live bug across the 9000-game fuzz. Latent trap if a
  future variant ever advances ≥ `count` seats or drops below 3 players; a clarifying
  paren (`direction * (steps % count)`) or a comment would remove the foot-gun.
- **Jump-in offers only `candidates[0]`** (`app.mjs:509-511`): the engine returns *all*
  eligible jumpers, but `handleJump` considers only the first (lowest seat, excluding the
  last player) — so among multiple eligible AIs, if the first declines, the window closes
  for the rest; jump-in is seat-deterministic rather than a true reflex race. The **human
  is never denied** (they're seat 0, always first when eligible), so impact is limited to
  AI-vs-AI priority realism. Likely an intended simplification; left in the report.

## Coverage ledger (pass 3)
`onu/` set to `2026-07-13`. No other rows touched.

## Next pass should cover (as written after the onu pass)
Never-reviewed, churn-heavy 2026-07 areas: **`tidecall/`** (hand-built vanilla-JS card
game — `lessons_learnt` already flags a render-flicker class there worth confirming),
then **`brilliancy/`**, **`shipshape/`**, **`span-of-control/`**, **`cruise-line/`** —
all `(never)`. Tier order remains NEVER-REVIEWED first, oldest `(never)` by risk.

---

# Pass 4 — `tidecall/` (deliberate scoped review)

Fourth bounded pass. Area picked from the ledger: **`tidecall/`** — a hand-built
vanilla-JS trick-taking card game (Estimation-Whist "tide" variant: 11 rounds,
bid-against-marks, a "high tide" surge trick worth 2 and a "slack" trick worth 0).
Slice: `engine.js` (595, pure rules + AI), `app.js` (1165, UI), `sw.js` (43, PWA),
plus `engine.test.js` + `validate-static.test.js`.

**Headline: clean bill of health. Zero bugs filed — and I did not manufacture any.**
This is the best-hardened slice reviewed so far.

**Verification I ran (evidence, not assertion):**
- Ran the suite: engine tests **8/8 pass** (incl. a 150-match AI autoplay) + static
  checks pass.
- Extended the autoplay into a **12,000-match fuzz** (4000 each of drift/current/gale),
  driving bids + cards via the engine AI, calling `validateState` **after every move**
  and additionally asserting: the dealer-hook forbidden bid is genuinely excluded from
  the legal set, every AI bid/card is legal, per-round marks balance to the hand size,
  and every match terminates at `matchEnd`. Result: **12,000/12,000 clean.** No engine
  correctness or robustness defect found.
- **Confirmed the two `lessons_learnt` issues for tidecall are already fixed** in current
  code (so those entries are now stale for this doc):
  - *Render flicker* (2026-07-11 lesson): `renderHand` (`app.js:452`) now **reconciles**
    — keeps a `Map` of id→node keyed by `roundIndex`, reuses nodes, removes only departed
    cards, and a code comment explicitly handles the "`S14` is the ace every deal" key
    collision the lesson called out. `renderStandings`/`renderTrick`/`renderTideTrack`
    are likewise keyed. The animation-restart flicker is resolved.
  - *Service-worker cache sharing* (2026-07-10 lesson): `sw.js` activate deletes only
    keys with its **own** `tidecall-` prefix (`sw.js:25`), so it can't wipe sibling
    almanac apps' caches. Correct.
- **Storage is fully guarded and validated** (`app.js:104-145`): every `localStorage`
  call is wrapped in try/catch, and `hydrateSavedGame` runs `E.validateState` before
  trusting a saved game — exactly the robustness onu (KILN-00030) and darmok
  (KILN-00013) were missing. No blank-page-on-blocked-storage risk here.
- **No XSS surface**: tidecall has no user-input text anywhere (seed/difficulty/dealer
  only); `innerHTML` sinks interpolate numbers + hardcoded seat names, and names are run
  through `escapeHtml`.

### Report-only (hardening notes — not filed)
- **SW app-shell fallback isn't gated to navigations** (`sw.js:41`): on a failed
  same-origin fetch, `caches.match('./index.html')` is returned for *any* request, not
  just `mode === 'navigate'` — the 2026-07-10 lesson's "reserve the shell fallback for
  in-scope navigations" clause. **Essentially unreachable today** (all six assets are
  precached, so a non-navigation cache-miss never happens for the current asset set), so
  it's a latent hardening note, not a bug. If a future non-precached sub-resource is
  ever added, gate the fallback: `if (event.request.mode === 'navigate') return
  caches.match('./index.html'); throw;`.
- **Ambient background canvas runs a perpetual `requestAnimationFrame` loop**
  (`app.js:1039`): by design, and it respects `prefers-reduced-motion` and pauses
  naturally when the tab is hidden (rAF halts), so no wasted cycles. No change needed;
  noted only for completeness.

### Stale-lesson note (for a future prune-lessons pass, not actioned here)
The two `lessons_learnt.md` entries above are now resolved *for tidecall* but remain
generally instructive for other docs — left in place; a `/prune-lessons` pass can decide
whether to trim the tidecall-specific detail.

## Coverage ledger (pass 4)
`tidecall/` set to `2026-07-13`. No other rows touched.

## Next pass should cover (as written after the tidecall pass)
Never-reviewed, churn-heavy 2026-07 areas: **`brilliancy/`** (`lessons_learnt` flags a
softlock class there — a `display:grid` defeating `[hidden]` — worth confirming against
current code), then **`shipshape/`**, **`span-of-control/`**, **`cruise-line/`** — all
`(never)`. Tier order remains NEVER-REVIEWED first, oldest `(never)` by risk.

---

# Pass 5 — `brilliancy/` (deliberate scoped review)

Fifth bounded pass. Area picked from the ledger: **`brilliancy/`** — a chess-puzzle /
"immortal games" game built on a real, from-scratch chess rules engine. Slice:
`engine.js` (372, movegen + FEN + SAN + perft), `scenes.js` (1005, scripted PGN lines
+ data), `game.js` (987, UI/animation), `index.html` (130), `style.css` (660), plus
three test files.

**Headline: clean bill of health. Zero bugs filed.** Like tidecall, this is a
carefully-built, well-tested slice — and the engine correctness is *definitively*
verified, not merely spot-checked.

**Verification I ran (evidence, not assertion):**
- Ran the suite: engine **34 checks pass** (FEN round-trips, SAN gen/parse,
  disambiguation, promotion, en passant, castling-rights, mate/stalemate) + scenes +
  static all pass.
- **perft to reference, one level DEEPER than the suite.** A chess movegen is
  definitively correct iff its perft node counts match the published reference values.
  The suite checks depths 3–4; I ran the canonical deep counts and all matched exactly:
  start d5 = 4,865,609 · Kiwipete d4 = 4,085,603 · Position-3 d5 = 674,624 ·
  Position-4 d4 = 422,333 · Position-5 d4 = 2,103,487. ~4–5M nodes each, exercising
  castling-through-check, en-passant-uncovers-check, promotion/underpromotion, pins and
  disambiguation. **Movegen is verified correct against reference.**
- **The `lessons_learnt` softlock (2026-07-11) is already fixed** in current code:
  `#overlay` is styled `display:grid` (`style.css:478`) AND carries the explicit
  `#overlay[hidden] { display: none }` override (`style.css:483`) — exactly the fix the
  lesson prescribed. I also checked the *other* `hidden` overlay, `#choices`
  (`index.html:117`): it sets no `display:` at all (only `position:fixed`), so the UA
  `[hidden]{display:none}` rule applies natively — no analogous softlock.
- **Storage guarded** (`game.js:25,28`): `store.read`/`write` wrap `localStorage` in
  try/catch (the robustness onu KILN-00030 lacked). **No XSS**: the only `innerHTML`
  sinks (`game.js:160,407,479`) interpolate a single piece-type letter into a static
  `<use href="#pc-X">` SVG template; all authored/dynamic text goes through
  `textContent`.

### Report-only (optional hardening — not filed)
- The suite's perft stops at depth 3–4. Adding one deeper case (e.g. Kiwipete d4 =
  4,085,603, ~2s) would pin the last sliver of movegen edge cases in CI. The current
  coverage already catches essentially all movegen bugs (Kiwipete d3 alone is a strong
  oracle), so this is a nice-to-have, not a gap — noted, not filed.

## Coverage ledger (pass 5)
`brilliancy/` set to `2026-07-13`. No other rows touched.

## Running scoreboard (passes 1–5)
darmok (12 bugs + 1 req) · news (2 new, +7 re-verified from the pass-1 misfire) ·
onu (1 low) · tidecall (0) · brilliancy (0). The last three passes hit well-built,
well-tested slices and were verified by seeded fuzz / reference perft rather than by
inventing findings — effort scaled to code quality, as intended.

## Next pass should cover (as written after the brilliancy pass)
Never-reviewed: **`shipshape/`** (2026-07 addition, ~9 JS files — a larger untested
surface than the last two engines), then **`span-of-control/`**, **`cruise-line/`**,
and the long `(never)` tail. Tier order remains NEVER-REVIEWED first, oldest `(never)`
by risk.

---

# Pass 6 — `shipshape/` (deliberate scoped review)

Sixth bounded pass. Area picked from the ledger: **`shipshape/`** — a personal
fitness-habit tracker (daily micro-duties, feedback-driven difficulty progression,
streaks/milestones, IndexedDB+localStorage persistence, JSON import/export). Slice:
`engine.js` (291, all the logic), `app.js` (872, render/UI), `storage.js` (175),
`content.js` (54, data), `sw.js` (50), + four test files. This was flagged as the
higher-yield target (thinner tests relative to code than the last two engines) — and
it delivered two confirmed defects.

### Confirmed findings (bugs filed)

| Sev | Finding | file:line | Bug | Effort |
| --- | --- | --- | --- | --- |
| Low | **Difficulty adjusts after a single feedback log, not the intended run.** `updateProgression`'s `recentFeedback.slice(-3).every(...)` / `.slice(-2).every(...)` are **vacuously true** on short history, so one "too_easy" bumps the level (harder targets) and one "too_hard" drops it — verified from empty history. Also `recentFeedback`/`recentStatuses` are filtered independently, so a status-only log misaligns them and the `easyRun` AND-condition stops comparing the same events. | `engine.js:153-160` | KILN-00031 | S |
| Low | **`longestStreak` miscounts across a DST transition.** It treats two dates as consecutive only when their noon-to-noon delta `=== 86400000` ms; across a London BST change that delta is 23 h / 25 h, so a genuine streak is split (verified: 3 consecutive maintained days across spring-forward → counted as 2). The sibling `currentStreak` uses DST-safe `setDate`, so only this stat is brittle. | `engine.js:213` | KILN-00032 | S |

### Verified strong (no defect — positive confirmations)
- **Storage is robust**: `hasLocalStorage()` wraps access in try/catch; `localLoad`
  runs `validateImportData` before trusting stored data; IndexedDB-first with a
  localStorage fallback adapter. No blank-page / corrupted-save risk.
- **No XSS**, even for a malicious import: `esc()` (`app.js:36`) escapes all five of
  `& < > " '` (stronger than darmok's, which misses `'`) and is applied to every
  dynamic interpolation into `innerHTML` (`duty.title`, `exercise.name`, feedback,
  milestone text, instructions…). A crafted import file can't inject markup.
- **`validateImportData`** rejects bad JSON, wrong version, malformed plans, unknown
  exercise ids, and unknown duty statuses.
- **SW correctly namespaced**: activate deletes only its own `shipshape-cache-` prefix.

### Report-only (hardening notes — not filed)
- **SW caches non-OK responses** (`sw.js:32-35`): the runtime-cache branch stores any
  fetched response without checking `response.ok`/status, so a transient 404/5xx (e.g.
  mid-deploy) for a non-precached URL could be cached and served (cache-first) until the
  cache version bumps. tidecall's SW guards this (`status !== 200`); shipshape doesn't.
  Low (all shell assets are precached, so runtime fetches are rare). Also, as with
  tidecall, the `./` fallback (`sw.js:36`) isn't gated to `mode==='navigate'`.

## Coverage ledger (pass 6)
`shipshape/` set to `2026-07-13`. No other rows touched.

## Running scoreboard (passes 1–6)
darmok (12 + 1 req) · news (2, +7 re-verified) · onu (1) · tidecall (0) ·
brilliancy (0) · shipshape (2). The prioritisation call paid off: after two
verified-clean engines, steering to the larger thinner-tested app surfaced two real
(if low) defects.

## Next pass should cover (as written after the shipshape pass)
Never-reviewed larger JS apps first: **`span-of-control/`** (`content.js` 3382 lines +
engine/app — a large hand-built slice), then **`cruise-line/`**, **`harbour-hustle-cruise-cup/`**,
and the `(never)` tail. Tier order remains NEVER-REVIEWED first, oldest `(never)` by risk.

---

# Pass 7 — `span-of-control/` (deliberate scoped review)

Seventh bounded pass. Area picked from the ledger: **`span-of-control/`** — a
seed-driven management-sim card game (3 meters leadership/team/you + headcount, 12
quarters, cards with left/right choices, ~endings). Slice: `engine.js` (237, pure
rules), `app.js` (420, UI), `storage.js` (51), `sw.js` (48), `content.js` (3382, data
guarded by `content.test`). The larger `content.js` is authored data validated by its
own test.

**Headline: clean bill of health. Zero bugs filed.** The engine is fuzz-verified and
the persistence/SW/render layers are all careful.

**Verification I ran (evidence, not assertion):**
- Ran the suite: **3/3 test files pass** (engine, content, static).
- Extended the engine test's autoplay into an **8000-game fuzz**, making
  engine-RNG-driven left/right choices and asserting after every choice: all three
  meters stay in `[0,100]`, `headcount ≥ 0`, `draw()` never returns null before an
  ending, `titleFor`/`quarterOf` never throw, and **every game reaches a valid ending**
  within the 144-choice cap (`FINAL_QUARTER × WEEKS_PER_QUARTER`). Result:
  **8000/8000 clean**, maxChoices = 144, with a rich, well-distributed ending set (both
  meter endings and many card-forced ones: cult 2710, burnout 1336, ic-return 1224,
  hr-sabbatical 712, peer-absorbed 700, … mass-resignation 30). No engine defect.
- **Checked the recurring low-severity classes — none present here:**
  - `storage.js`: every `localStorage` access is try/catch-guarded (load + save); a
    private/blocked-storage browser degrades to a per-session game, never a crash.
  - `sw.js`: activate deletes only its own `span-of-control-` prefix (namespaced), and
    the fetch handler is **cache-first with no runtime `cache.put`** — so it can't cache
    a non-OK response, and there's no app-shell-fallback-for-subresources issue either.
    The cleanest SW of the apps reviewed.
  - No `.every()`-on-short-array logic; the engine uses no `Date`/`Math.random` at all
    (fully seed-driven), so no DST-day-delta assumption exists.
- **No XSS**: every `app.js` `innerHTML` use is `= ""` (clear-then-rebuild); content is
  built with `createElement` + `textContent`. No user input, no dynamic-string HTML sink.

### Report-only (very-low nit — not filed)
- `applyEffects` (`engine.js:148-152`) records the **requested** delta
  (`deltas[key] = effects[key]`) while clamping the meter to `[0,100]`, so a post-choice
  readout could show e.g. "+10" when a near-cap meter only moved +5. Cosmetic and
  arguably intended (it shows the authored effect); noted, not filed.

## Coverage ledger (pass 7)
`span-of-control/` set to `2026-07-13`. No other rows touched.

## Running scoreboard (passes 1–7)
darmok (12 + 1 req) · news (2, +7 re-verified) · onu (1) · tidecall (0) ·
brilliancy (0) · shipshape (2) · span-of-control (0). **17 bugs + 1 req across 7
areas.** The hand-built engines are consistently high quality (fuzz/perft-verified);
the yield sits in the data-heavier apps (news, shipshape).

## Next pass should cover (as written after the span-of-control pass)
Never-reviewed larger JS apps: **`cruise-line/`** (has a tested sim core +
`validateState` per its CLAUDE.md — likely robust but worth the pass), then
**`harbour-hustle-cruise-cup/`**, **`estimation-whist-game/`**, and the `(never)` tail.
Tier order remains NEVER-REVIEWED first, oldest `(never)` by risk.

---

# Pass 8 — `cruise-line/` (deliberate scoped review)

Eighth bounded pass. Area picked from the ledger: **`cruise-line/`** ("Wake and
Fortune") — the largest hand-built slice reviewed: a cruise-line tycoon sim,
`engine.mjs` **1257 lines** + `app.mjs` 979 + `content.mjs` 792 + `storage.mjs`,
`sw.js`, `guidance.mjs`, with 23 tests. It already carries `ALM-REQ-KILN-00001`
(save-restore hardening).

**Headline: clean bill of health. Zero bugs. This is the exemplar of the codebase** —
the reference implementation for the very issues flagged report-only elsewhere.

**Verification I ran (evidence, not assertion):**
- Ran the suite: **23/23 pass** (engine + guidance + static).
- **4000-game fuzz** across every difficulty × focus (standard/hard × family/premium/
  luxury/adventure/value) × 400 seeds, interleaving legal player actions (borrow,
  setMarketing, setMarketPrice, orderShip) with the `advanceQuarter` tick, asserting
  `validateState() === []` after **every** action and every quarter, the company ledger
  (cash/debt/reputation/sustainability) stays finite, and the serialised state
  round-trips valid. Result: **4000/4000 clean**; every game terminated at quarter 24
  as `finished`. No engine defect, no invariant violation, no non-finite number.
- **Storage** (`storage.mjs`): `storageAvailable()` probes with a real set/remove in
  try/catch; every load/save guarded; `loadGame` checks version **and**
  `validateState(parsed).length === 0` before trusting a save (the KILN-00001 hardening,
  live).
- **XSS closed**: the one real user-input surface (company name ≤36 chars, ship name ≤42,
  livery regex-validated to `#rrggbb`) is handled correctly — names render via
  `textContent`, or through the `e()` escaper at **every** `innerHTML` boundary
  (verified at lines 136, 291, 347, 396, 428, 435, 659, 662, 676, 678). Even the
  message strings that interpolate a raw `ship.name` (`confirmAction`, the `currentRisk`
  advisory) are escaped at the render boundary (`confirmAction` `e()`s all its args;
  the advisory title is `e()`d at app.mjs:396). A crafted save can't inject markup.

**cruise-line's `sw.js` is the gold-standard reference.** It is the only reviewed SW
that closes all three recurring issues at once: it deletes only its own
`wake-and-fortune-` prefix (namespaced), **scope-checks** each request
(`url.pathname.startsWith(scope.pathname)`), caches **only** OK responses
(`if (response.ok) cache.put(...)`), and **gates the app-shell fallback to navigations**
(`event.request.mode === "navigate" ? cache.match("./index.html") : Response.error()`).

### Cross-cutting note (report-only — not filed as a req)
The report-only SW hardening notes from earlier passes — tidecall (`sw.js:41`, fallback
not nav-gated) and shipshape (`sw.js:32-35`, caches non-OK responses; fallback not
nav-gated) — have a ready fix: **adopt cruise-line's `sw.js` shape**. Low-value (those
paths are near-unreachable given precached assets), so left as a note here rather than a
tracked req; a future SW-consistency sweep could align all the document service workers
to this pattern.

## Coverage ledger (pass 8)
`cruise-line/` set to `2026-07-13`. No other rows touched.

## Running scoreboard (passes 1–8)
darmok (12 + 1 req) · news (2, +7 re-verified) · onu (1) · tidecall (0) ·
brilliancy (0) · shipshape (2) · span-of-control (0) · cruise-line (0). **17 bugs + 1
req across 8 areas.** Four consecutive fuzz/perft-verified-clean engines
(tidecall, brilliancy, span-of-control, cruise-line) — the hand-built engine layer of
this codebase is uniformly high quality; the review yield is in the data/content-heavy
apps.

## Next pass should cover
Never-reviewed remaining apps: **`harbour-hustle-cruise-cup/`**, then
**`estimation-whist-game/`**, **`estimation-whist/`**, **`model-railway/`**,
**`rail-control/`**, and the `(never)` tail. Tier order remains NEVER-REVIEWED first,
oldest `(never)` by risk. Given the engine-yield pattern, weight toward the apps with
the most authored content/data.
