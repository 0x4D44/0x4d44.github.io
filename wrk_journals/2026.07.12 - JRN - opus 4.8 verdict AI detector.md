# 2026.07.12 — Opus 4.8 Verdict (on-device AI-writing detector)

Branch: `claude/pangram-detector-almanac-0jppjj` · PR #26

## Goal / non-goals
- **Goal:** build an almanac document that mirrors Pangram's AI-writing-detector UX
  but runs fully client-side and is *honest* about being an explainable heuristic
  rather than a trained classifier. Then keep building/hardening it while operator
  is AFK.
- **Non-goals:** no server, no real neural model, no accuracy claims. It's a
  writing-style *signal*, not proof.

## Mode
Operator invoked page-1/Z **AFK MODE** ("open pr and then go build"). Working
autonomously: reversible defaults, no pausing, atomic commits on specific paths,
push only to the already-authorized feature branch (updates PR #26 — reversible,
not merged, not main). STOP-and-log anything irreversible / architectural.

## Log

### Pass 0 — initial build (done, committed 850ed8b, pushed)
- `opus-verdict/index.html`: self-contained engine + UI. Eight signals
  (burstiness, machine vocabulary, transition density, formulaic openers,
  em-dash, rule-of-three, hedging, lexical repetition), weighted → 0–100 verdict
  with confidence gauge, signal breakdown, per-sentence highlighting + tooltips,
  three sample texts, methodology panel.
- `index.html`: new `ill-verdict` sprite symbol. `data.js`: catalog entry
  (software + science), newest-first.
- Verified: engine test (human 3 / mixed 67 / AI 75); Chromium smoke test (render,
  gauge, signals, highlighting, tooltips, homepage card + link) — no page errors.
- PR #26 opened against main.

### Pass 1 — hardening (in progress)
Bounded enhancement pass while AFK. Plan: (1) offline validation harness over a
labelled human+AI corpus to confirm class separation; (2) add an n-gram
repetition signal + calibrate *only if* it improves separation without
regressing; (3) fix gauge readout overlap; (4) add a Copy-report button.
Decisions recorded below as they happen.

### Pass 1 results — engine hardening (done)
Built an offline validation harness (scratchpad, not shipped — repo has no test
suite by design) over a labelled corpus: 6 human (Dickens, Austen, Hemingway,
casual anecdote, terse tech note, Reddit-style), 5 AI (stock essay, listicle,
chatbot how-to, LinkedIn post, explainer), 2 mixed; plus 4 adversarial-human
cases (corporate-speak, formal memo, stray-buzzword).

**Finding:** initial engine had perfect precision (humans 0–13) but weak recall —
genuinely-AI paragraphs with varied rhythm slipped into "uncertain"/"human"
(one at 26). Every AI sample maxed `vocab`; the weighted average diluted it.

**Decision — corroborated escalation** (not a raw vocab floor). First tried a
vocab-only floor: lifted recall but FLAGGED a human corporate-speak message and a
formal human memo as "Likely AI" — unacceptable (false positives are the costly
error and the tool's whole pitch). Retuned so:
  - stock vocabulary ALONE only lifts to the "uncertain" band (≤48);
  - "Likely AI" requires vocab AND a second sustained signal (transition / triad /
    formulaic-opener / em-dash) — `min(vocab, max(those))*74`.
Also softened the vocab curve (÷14 → ÷18) and gated on `W≥40 && tellsAbs≥3` so a
lone stray buzzword can't move a short note. Escalation only ever LIFTS, so clean
human text (vocab≈0) is untouched.

**Outcome:** clean humans 0–13 (unchanged); corroborated AI → 74 "Likely AI";
vocab-only-AI and ambiguous-human both → 48 "Uncertain" (the honest answer).
Precision preserved, recall materially up. Verified via harness + adversarial set.

**Deferred (logged, not done):** a dedicated n-gram-repetition signal — the MATTR
`rep` signal already occupies that space and fired ~0 on the corpus; a new signal
was not justified by the data. Reversible to revisit.
