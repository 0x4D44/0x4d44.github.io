# JRN — Chief Engineer game (cruise ship engine-room sim)

Task: new almanac game — player is the chief engineer of a cruise ship. Engine
lineup, fuel/bunkering, emissions, maintenance scheduling, failures up to
disasters. Stepped levels from small ship to mega-liner. Tutorial/guidance.
Worktree: `task/20260716-DEV-HUM-chief-engineer-game`.

## 2026-07-16

- Worktree created off origin/main via `deltic start-task`.
- Launched 3 research agents (lean process cap): (a) chief engineer role +
  power-plant/system numbers, (b) failures/incidents/disasters + causal chains,
  (c) sim-game design references (core loop, progression, control-room UI).
- Read repo conventions: shipshape as exemplar (pure engine.js + node tests +
  validate-static; ES modules; no external URLs). Root package.json chains every
  doc's tests into `test`/`build` — must add mine there. Sprite symbols are
  200×120 line-art, stroke currentColor.
- Decision (pending design review): NO service worker / PWA for this game —
  avoids the shared-origin CacheStorage footgun from lessons_learnt and keeps
  scope down. brilliancy ships without one too.
- Key lessons_learnt to honour: reconciling render (no replaceChildren per tick —
  animation restarts/flicker), explicit `#el[hidden]{display:none}` when styling
  display, playtest with real hit-testing (elementFromPoint), phone viewports
  360×640 / 390×844.
- HLD v1 written and committed (6f9a7f9). Adversarial review: 3 reviewers
  (game/UX, sim/scope, repo/integration), then full cross-refutation. 24
  findings → 20 survived (many narrowed), 4 refuted. Highest-value catches:
  fuel-burn formula off 60× (t/h labelled t/min); board topology must be in
  core state from day one (retrofit would rewrite saves/tests); warning phases
  must be authored in game-hours + speed drop on ANY new alarm, else the
  causality ladder is invisible at 32×; Safety star hid the signature
  black-start from good players → scripted L2 drill; event phase-machine
  contract (instance keying, regression semantics, concurrency caps) written
  out; browser.test.mjs (onu pattern) added to the test plan; accessibility
  section added (flash ≤ 2 Hz, reduced-motion, aria-live, keyboard).
  Notable refutations: retry-from-crisis rejected (crisis states often
  pre-doomed; incident-report→fresh-run is the pedagogy); comfort scored as
  voyage average deliberately (an early trough dilutes — a floor rule would be
  the unwinnable one).
- HLD v2 committed (f0b0a9d). External Codex review (gpt-5.6-sol, xhigh)
  launched, bounded 30 min.
- Codex sandbox broken on this box (codex-windows-sandbox-setup.exe missing) —
  first review run couldn't read files; worked around by inlining the whole HLD
  into the prompt via stdin. Verdict BUILD-WITH-FIXES; all 10 findings folded
  (two real criticals: level arithmetic didn't close; voyage movement
  unspecified). HLD v3 SELF-APPROVED (582aa6b).
- Build: engine + content + tests first (039583c). Two notable engine bugs the
  playthrough oracle caught: (1) `availablePropMw` speed inversion ignored the
  weather factor → chosen speed demanded up to 30% more MW than the bus could
  give → overload/shed oscillation in storms and a 13-tick blackout/recover
  loop post-fire; (2) propSplit not renormalized after board loss. Also moved
  drift hazards from level to leg (blackout mid-Atlantic shouldn't ground you
  on "Ushant"). Bot bug: telegraph-ack rule demanded N+1 at ordered speed —
  ship sat at 0 kn until dry; engine power-caps actual speed so ack is safe.
- UI + integration (a4b2dd0). Browser test (onu CDP pattern) caught a real
  sequencing issue on first run: stale autosave from the prior viewport created
  a Resume card that the generic selector clicked.
- Screenshot playtest (manual-snap.mjs → wrk_docs/snaps/, not committed):
  caught mentor-toast leakage across level transitions, almanac-pill overlap
  of the header, phone tab clipping, objective checkbox wrapping. All fixed
  (de27059). Headless rAF is throttled (~2 ticks/s) — added __chief.step(n)
  for deterministic harness driving. Bonus: the slow snap-harness bot fumbled
  an ER1 fire on L6 and the game degraded exactly as designed (board lost,
  ship limps on ER2 at 16.4 kn) — SRtP behavior verified under real failure.
- Chrome extension (claude-in-chrome) not connected on this box — interactive
  playtest done via headless CDP + screenshot review instead.
- Code review (workflow, high effort, 36 agents): 33 verified findings → 10
  distinct confirmed defects, ALL fixed with regression tests (4968665).
  Standouts: bus-overload alarm flapped clear by a healthy island (forced 1×
  every game-minute); fuel-margin KPI counted the unburnable grade; a sheet
  overlay could permanently cover the debrief; objectives latched vacuously at
  tick 1 (armed L4's hazard pool during the tutorial); ECA fines were never
  assessed on PSC-less routes — I had spotted that one mid-build and then
  forgot to implement it. Humbling and exactly why the review layer exists.
- Simplify pass (3 agents: redundancy/efficiency/altitude → afba783): deleted
  speculative machinery, single-sourced the cube law (bot + content oracle now
  import the engine's formula — a re-typed copy would silently detach the
  oracle), structured alarm components instead of regex-parsed tile ids,
  bounded log/alarm state, render only on sim advance, wired the PMS assist
  toggle the HLD promised. SKIPPED with rationale: CDP harness dedup between
  browser.test and manual-snap (different purposes; touching gate infra
  pre-integration adds risk), and a full per-tick memoization cache (stale-
  cache risk for a micro-win; the 44k-tick suite already runs in seconds).
- Gotcha for posterity: native confirm() dialogs block headless CDP entirely —
  any harness must subscribe Page.javascriptDialogOpening and auto-accept.
- Final gates: full root npm test + build green (incl. onu + all sibling doc
  suites); playthrough tick-identical before/after simplify.
