# Tellylist setup guide

Added a reader-facing installation guide at `/tellylist-setup/`, listed on the Almanac Tools shelf. The app remains independently hosted by the Tellylist repository at `/tellylist/`; no app bundle, service worker or account information was copied here.

The public app address returned HTTP 404 during this work. The guide therefore says it is still being set up and makes installation conditional on Arthur confirming readiness. It covers Safari Home Screen installation of the app (not this guide), emailed sign-in, a second device, first-use tips and simple help. Once the service is ready, update the guide status and the catalogue tagline together.

Validated catalogue uniqueness, URL, illustration and shelf membership; JavaScript syntax and diff whitespace checks pass. A temporary scoped copy of the existing Chrome responsive harness tested 320, 390, 768 and 1280 pixel widths, the shared Almanac button and keyboard skip-link reachability. Initial checks failed before the page existed and exposed a skip-link overlap; final checks pass. Reviewed phone and desktop screenshots. Temporary scripts and images were removed. Apple installation instructions were checked against the linked official iPhone and iPad guides.

No hosted app configuration or actual iOS installation was performed. The full unrelated catalogue/game suite was not run for this static page addition.
