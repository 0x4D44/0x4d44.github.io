# 2026.06.17 — JRN — Doc & version reconciliation pass

One bounded reconciliation pass over the static-site repo. **Result: already in sync; no corrections needed.**

## What this repo has (scope of the pass)
- No root version manifests (`package.json` / `Cargo.toml` / `VERSION`): the site is served verbatim, so there is no version to keep in lockstep. Step 1 (version lockstep) N/A.
- No quality/capability registry as such — **`data.js` (`window.ESSAYS`) is the de-facto catalog registry**, so a published document directory without a catalog entry is the equivalent "new capability never registered" drift. Checked (step 3 analog).
- No daily report / known-issues ledger. Step 4 N/A.

## Checks run
1. **Catalog completeness** — every directory containing an `index.html` is referenced by a `data.js` URL. `comm` of doc-dirs vs catalog URL final-segments: empty diff → no unregistered documents.
2. **CLAUDE.md live-state claims** — all verified against the tree:
   - `localStorage` key `0x4d44.listing.v1` → present in `app.js:10`.
   - `ill-diesel` illustration fallback → present in `app.js` (rows + cards).
   - React apps load React 18 UMD + `@babel/standalone` from unpkg, transpiled in-browser via `type="text/babel"` → confirmed in `picoem/index.html`.
   - All 14 example/source slugs named in CLAUDE.md exist as directories.
   - `about.html` reuses `data.js`/`ESSAYS` → confirmed.
   - "no build system / no package.json / no test suite" → still true.

## Findings
None. Nothing was out of sync; no edits to docs or version metadata beyond this journal.

## Adversarial verification
Three refutation attempts on the "all in sync" conclusion, all refuted: (a) unregistered new doc — empty `comm` diff; (b) stale CLAUDE.md fact — every cited fact re-verified; (c) diverged version manifest — no manifests exist.
