# Overnight code review pass — 2026-07-03

Mode: read-only review pass with docs/ledger writes only.

I created a task worktree, fetched `origin`, and seeded `wrk_docs/review-coverage.md` because the repo did not yet have a coverage ledger. All derived top-level areas started as `(never)`. I selected `japanese-travel-rpg/` for this pass because it was never reviewed, recently changed, and high-risk: interactive DC app, PWA cache, mobile frame, and service-worker behavior.

I reviewed the slice with parallel lenses for correctness, security, performance, reliability, maintainability, and test coverage, then ran three skeptic passes over the candidate list. The survivor set was A through H: offline/CDN boot dependency, overbroad cache deletion, roleplay content overlay desync, positional content IDs, inverted review empty state, review of locked future material, malformed bottom-nav bindings, and shipped persistence bypassing the browser-store abstraction.

I filed seven current defects in `bugs/`:

- `ALM-BUG-FLUXHOMEARPA-00001` — offline boot depends on CDN runtime assets.
- `ALM-BUG-FLUXHOMEARPA-00002` — service worker deletes unrelated origin caches.
- `ALM-BUG-FLUXHOMEARPA-00003` — roleplay safe answers disappear after authored content overlay.
- `ALM-BUG-FLUXHOMEARPA-00004` — review empty state is gated by the active-card condition.
- `ALM-BUG-FLUXHOMEARPA-00005` — review exposes locked future material to new users.
- `ALM-BUG-FLUXHOMEARPA-00006` — bottom-nav icon bindings are malformed.
- `ALM-BUG-FLUXHOMEARPA-00007` — shipped persistence bypasses the browser-store fallback.

I captured one Draft requirement in `reqs/`: `ALM-REQ-FLUXHOMEARPA-00001`, for stable authored phrase identities and saved-progress merge behavior. I did not file the positional-ID issue as a current bug because it is a durable content-contract risk that needs a future content edit to corrupt saved progress.

Refuted or held findings: `app.js` security issues are not exploitable through the shipped DC page because `index.html` does not load `app.js`; duplicate self-fetch and render recomputation are real observations but not defect-level at the current fixed content size; SVG-only install icons and dictation privacy wording did not survive the confirmed-finding bar.

I did not run the app, run tests, or run the repo's exploratory-test harness, per the pass instructions.
