# 2026.07.13 — JRN — Overnight code-review pass 8 (cruise-line)

## What I did
Eighth bounded review pass. Picked `cruise-line/` ("Wake and Fortune", never-reviewed —
largest hand-built slice, engine.mjs 1257 lines). Read engine API + storage + SW + the
app render/escaping; ran the suite; ran a 4000-game fuzz. **Zero bugs — clean bill, and
the exemplar of the codebase.** Upserted `cruise-line/` → 2026-07-13. Worktree:
`D:/worktrees/0x4d44.github.io/20260713-CR-CLD-review-cruise-line`.

## Result: exemplary across the board
- Suite 23/23 pass.
- 4000-game fuzz (2 difficulty × 5 focus × 400 seeds), interleaving legal player
  actions with advanceQuarter, asserting validateState()===[] after every action + every
  quarter, ledger finite, serialised round-trip valid. 4000/4000 clean; all terminate at
  quarter 24 "finished".
- Storage: storageAvailable() probe + all try/catch; loadGame checks version AND
  validateState before trusting a save (KILN-00001 hardening, live).
- XSS closed: user input (company name, ship name, livery) via textContent or the e()
  escaper at EVERY innerHTML boundary — confirmed confirmAction e()s its args and the
  currentRisk advisory title is e()'d at app.mjs:396. Crafted save can't inject.
- sw.js is the GOLD STANDARD: namespaced + scope-checked + OK-only caching +
  nav-gated app-shell fallback. Closes all 3 recurring SW issues at once.

## Cross-cutting note
cruise-line's sw.js is the fix template for the report-only SW notes filed against
tidecall (fallback not nav-gated) and shipshape (caches non-OK; fallback not nav-gated).
Left as a report note, not a req — those paths are near-unreachable (precached assets).
A future SW-consistency sweep could align all document SWs to this pattern.

## Decisions / pattern
- 0 bugs. FOUR consecutive fuzz/perft-verified-clean engines now (tidecall, brilliancy,
  span-of-control, cruise-line; onu had 1 low). The hand-built engine layer is uniformly
  excellent. Confirmed yield pattern: file-count/data-volume, not engine-size, predicts
  yield — news (255→355 articles) gave 9, shipshape (data-driven progression) gave 2,
  the pure engines give 0-1.
- Steer for the driver: remaining hand-built apps (harbour-hustle, estimation-whist-game,
  model-railway, rail-control) next; then the content docs. The static-prose essays are
  lowest yield and can be batch-covered late.

## Next
Next pass: **`harbour-hustle-cruise-cup/`**, then estimation-whist-game, estimation-whist,
model-railway, rail-control, and the `(never)` tail.
