# JRN — med-cruise: four new games + muster card

## 2026-07-02

**Task:** Add 4 port-themed deck games + the "essentials" features Arthur liked.

**STEP 0** — worktree `20260702-DEV-CLA-medcruise-games-and-essentials` off
`origin/main` (`80a6f1a`).

**STEP 1 UNDERSTAND — key discovery that changed scope:**
Read `games.dc.html`, the `Base` engine + Shuffleboard in
`cruise-games-engines.js`, and `index.html`. Found the Extras tab **already
implements** packing checklist, currency+tipping tips, phrasebook
(ES/CA/IT), and a journal — all complete. Itinerary is entirely Eurozone with
**no French port**, so the existing phrasebook/currency are already correct.
→ Three of the four proposed "essentials" already exist. Rebuilding = duplicate
work. Corrected scope to **4 new games + only the muster card** (genuinely
absent — no muster/safety content anywhere).

Engine contract confirmed: `new Game(canvas,{sfx,onState})`, `start/reset/
destroy`, `onState` on discrete change; `Base` owns RAF + pointer + HUD; canvas
640×920; hub persists bests to `localStorage["cg_best"]`.

**STEP 2 DESIGN** — wrote HLD (`wrk_docs/2026.07.02 - HLD - …`). 4 games as
separate small engines; muster card mirrors the journal (textarea + localStorage);
SW `VERSION` bump only (no new files → CORE precache list unchanged). Decision
log D1–D7; AC1–AC8; risks R1–R3.

**STEP 3 ADVERSARIAL REVIEW** — 3 reviewers (simplicity/scope, integration,
offline/testability) + refutation. Survivors folded as Δ1–Δ8:
- **Δ1 (headline):** my first Pisa spec (lean/centre-of-mass/sway/topple) was
  Shipwright reskinned. Re-specced to overhang-**slice** (Tower-Bloxx) — distinct
  mechanic, simpler code. D4 rewritten.
- **Δ2:** generic pointer poke can't reach AC3 states → scripted-input behaviour
  drive. ("Split into several integrations" rejected — brief wants all 4 now.)
- **Δ3:** `recordBest`+`bestStr`+emit-field 3-way coupling; Mosaic best pinned to
  patterns-solved (higher-better).
- **Δ4:** quiz integrity check at module load, sets `window.__cruiseGamesOk`.
- **Δ5:** git gates for "no new files / support.js untouched / sw.js VERSION-only".
- **Δ6:** muster inputs **uncontrolled** (1 s `forceUpdate` would eat keystrokes).
- **Δ7:** `node --check` engine before commit.
- **Δ8:** accept play-tab/hub tile duplication; keep in sync.
Rejected: canvas→DOM quiz (pointer model hit-tests fine); `{cache:'reload'}`
(network-first self-heals — R4). SW claim verified correct by 2 reviewers.

**STEP 4 EXTERNAL REVIEW (Codex gpt-5.5, xhigh, read-only)** — verdict
proceed-with-changes. Folded as ΔC1–ΔC6:
- ΔC1: AC3 still said Pisa "topples" — fixed to slice/perfect/miss/over.
- ΔC2: no skipWaiting → v2 activates next load (deliberate); documented + added
  close/reopen activation check. No SW code change.
- ΔC3: quiz ARIA/keyboard **declined** (whole arcade is canvas, no a11y anywhere;
  would re-add the declined DOM special-case); accepted the cheap layout check.
- ΔC4: build+smoke each engine serially, one integrated commit.
- ΔC5: muster test waits >1s past a tick before reload.
- ΔC6: safety wording defers to ship crew/PA; "seven or more short + one long".
Codex independently confirmed the scope correction is sound.

**STEP 5 SIGN-OFF** — pre-approved by Arthur (AFK). Doc marked SIGNED-OFF. Will
commit but NOT push (not requested); hand back ready to integrate.

**STEP 6 IMPLEMENT** — 4 engines appended to cruise-games-engines.js (Pisa slice,
Colosseum dodge, Mosaic tile, Quiz trivia) + shared `wrapLines` + QUIZ bank +
module-load integrity check; hub wired (META/CLASSES/recordBest/bestStr/games,
"Eight ways", count 8); index.html Play tiles ×4 + muster card (uncontrolled
inputs, `hm_muster`, saveMuster); sw.js VERSION v1→v2. `node --check` clean.

**STEP 8 VALIDATE (pre-review) — all green:**
- Behaviour oracle (throwaway `/tmp/mc-engine-test.js`, stub-canvas Node drive):
  17/17 — each new game reaches its end-states, emits the score field matching
  recordBest/bestStr (Δ3), quiz integrity flag true (Δ4), draw() smoke no-throw.
- Git gates (Δ5): only 4 files M (no new files, AC7); sw.js diff = VERSION line
  only; support.js/vendor/fonts untouched (AC5).
- Headless Chrome: hub renders all 8 games + "Eight ways", 0 console errors (AC1/
  AC8); muster card renders on Extras with correct safety wording, 0 errors (AC4);
  **offline cold-load** with server down renders the 4 NEW games → v2 precached the
  updated engine (AC6). Probe file removed; git clean.

**STEP 7 CODE REVIEW + SIMPLIFY** (equivalent passes — 2 lean agents):
- Correctness review: **0 blockers, 0 issues.** Independently confirmed all 4
  field-coupling triples (Δ3), achieved-streak emit, no per-frame emit, Colosseum
  can't get stuck, muster uncontrolled-input correctness. 3 cosmetic nits left as-is
  (round# during banner; 1-frame-lenient catch; best-on-over) — all match the
  existing engine contract.
- Simplicity review: flagged the Δ4 module-load integrity IIFE as dead
  (`__cruiseGamesOk` write-only in the repo). **Applied: deleted it** (reversing
  Δ4's production mechanism); bank validation now behavioural in the throwaway
  oracle (answer every question → streak==len). Also hoisted a loop-invariant
  `c.font` in `Quiz._layout`.
- Re-validated: node --check clean; oracle 17/17 (incl. full-bank quiz shape);
  headless hub renders 4 new games, 0 console errors; git gates hold.

**STEP 8 COMMIT** — committing on the task branch; NOT pushing (not requested).

