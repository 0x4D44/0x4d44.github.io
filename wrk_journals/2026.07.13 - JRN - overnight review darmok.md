# 2026.07.13 — JRN — Overnight code-review pass (darmok)

## What I did
One coverage-ledger-driven review pass. Picked `darmok/` (largest never-reviewed
hand-written logic, thinnest tests, recent churn). Fanned out 7 lenses via a
hand-rolled deep-review workflow with adversarial verify per finding; also
reproduced the top engine defects myself directly against `engine.js`. Filed 12
darmok bugs (KILN-00006..00017) + 1 Draft req (KILN-00002). Reconciled the coverage
ledger. Worktree: `D:/worktrees/0x4d44.github.io/20260713-CR-CLD-review-darmok`.

## Surprises / decisions
- **The `/deep-review` workflow reviewed the wrong area first.** A fresh worktree off
  `origin/main` has an empty diff, and deep-review is diff-oriented, so all 37 agents
  reviewed the most recent *commit* (the 100-article `news` drop) instead of my picked
  `darmok/` slice. Lesson: for an area review (not a diff review), you must hand-roll
  the fan-out with the explicit file slice as args — a diff-based workflow will silently
  retarget. Recorded in lessons_learnt.
- **The misfire produced real, verified findings anyway.** Rather than discard 18
  adversarially-confirmed news defects, I filed the 7 distinct ones (KILN-00018..00024)
  with clear provenance, and deliberately left `news/`'s ledger row `(never)` — I did
  not vet news's by-design set, so it still needs its own pass. Filing verified defects
  ≠ claiming the area reviewed.
- **My own guesses kept getting refuted** (unanswerable typeback; cascade-timer leak;
  JP_RE lastIndex) — good discipline reminder: I stopped guessing and ran differential
  oracles over the pure engine instead. That is what surfaced the strongest finding
  (KILN-00006): 20/513 words reject the rōmaji the app teaches, with three distinct
  root causes in `normalizeAnswer`, each reproduced deterministically.
- **Severity discipline:** the security lens filed the localStorage→innerHTML path as
  high (stored XSS). The skeptic corrected to low — static personal site, no auth, no
  secret, and the same DK.load defect's *brick* face is the more likely real harm. Kept
  the skeptic's low. The `based-on-truth` disclaimer contradiction (news) is the one I'd
  most want a human to look at — it's a site-integrity issue on a live public page.

## Verification notes
- KILN-00006 (ー/apostrophe/づ): reproduced with `node` against `engine.js` —
  `normalizeAnswer("こーひー")`→こひ vs `romajiToHiragana("koohii")`→こおひい; `ten'in`→てにん;
  `tsuzukemasu`→つずけます. Also confirmed SRS demotes on the false-reject.
- KILN-00007/00008/00012 confirmed by direct grep of the handler chain / reset object /
  `P.days` write sites — deterministic, no vote needed.
- Everything else: lens finding + a skeptic that actually ran (no `needs_reverify`
  survivors treated as confirmed).

## Next
Next pass: **`news/`** deliberately (confirm the filed bugs against a scoped review,
vet by-design), then the other 2026-07 churn areas still `(never)`: onu, tidecall,
brilliancy, shipshape, span-of-control, cruise-line.
