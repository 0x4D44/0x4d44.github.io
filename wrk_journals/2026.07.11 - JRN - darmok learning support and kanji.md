# JRN 2026-07-11 — DARMOK learning support layer + kanji breakdowns

Feature build off playtest feedback. Design of record: `wrk_docs/2026.07.11 - HLD -
DARMOK learning support layer and kanji breakdowns.md`. Worktree branch
`task/20260711-DEV-HUM-darmok-kanji-breakdowns-and-data-support`.

## Context
- DARMOK is a live almanac doc (`darmok/`): vanilla JS, no build. Data model:
  vocab = `[jp(furigana), kana, romaji, en, type]`; lessons have `briefing[]`,
  `vocab[]`, `exercises[]`, `review`. `DK` (engine.js) holds SRS, exercise gen,
  audio, storage. `app.js` is the UI (delegated click handler at ~L875; views
  bridge/missions/lesson/drill/reference/log; `render()` full-redraws `#app`).
- 400 unique kanji across 60 lessons / 513 vocab entries.

## Plan (see HLD)
A. Data support layer: gentler first-exposure in `buildSession`; ASK DATA progressive
   hints; struggle detection → Data comm panel; onboarding tour.
B. Kanji: `DK.KANJI` registry in `darmok/kanji.js`; tappable kanji → breakdown modal;
   KANJI reference tab.

## Log
- Set up worktree, HLD, journal.
- Launched workflow `darmok-kanji-authoring` (wf_1f67f9f5-303): 16 batches ×
  (author → adversarial verify) writing verified JSON to
  `C:/Users/marti/AppData/Local/Temp/darmok-kanji-build/verified-*.json`. Verify pass
  is strict on JP-vs-Chinese notes (delete if unconfirmed). ASSEMBLE those into
  `darmok/kanji.js` when done.
- (first fix commit already on main from the earlier integration: mobile tbar overflow,
  reset try/catch, boot double-render — those are shipped; this branch builds on top.)

## Next
1. Implement support layer (no kanji dep) → commit green.
2. Assemble kanji.js from verified batches → registry + modal + tappable + KANJI tab.
3. `darmok/kanji.test.mjs` + wire into root gate; browser-verify at 360/390; integrate.
