# 2026.07.19 — JRN — win2k: "Boot to Desktop" story site

Added `win2k/` — an eight-page interactive essay on how mddosem was made to boot
Windows 2000 Professional SP4 to a usable desktop (28 May → 19 July 2026,
MDD-BUG-KILN-00004), published hours after the acceptance was recorded (03:27).

## What was built

- Eight pages on the world-in-640k house pattern: `index` (overview + saga
  timeline), `machine` (the emulator + boot-chain stepper), `loop` (restart loop
  + hypothesis board), `wires` (interrupt delivery + INTRQ wire stepper),
  `forensics` (KD/RAM-dump census + census explorer), `dialog` (the invisible
  dialog + black-screen X-ray), `cpu` (the four fixes + STI-shadow and PSE
  page-walk steppers), `desktop` (final night + acceptance + evidence).
- Shared foundation: `w2k.css` (cold NT-blue palette; Win2K chrome recreations;
  four-pane flag motif; chart series + 4-slot fix palette validated with the
  dataviz checker against `--surface #111a2c`), `w2k-lib.js` (canvas harness +
  stepper helper, zero dependencies), `w2k-data.js` (timeline, census,
  hypotheses, fixes, final night, evidence — transcribed from the mddosem
  ledger/journals/artifacts; do not hand-edit numbers).
- Catalog registration: `data.js` entry (slug `win2k`, 19,800 words, tags
  software/engineering/history), `ill-win2k` sprite symbol, Machine Room shelf.

## Method

Three workflow passes over the mddosem record (bug ledger MDD-BUG-KILN-00004,
wrk_journals 2026.06.23 → 2026.07.15, the endgame branch
`task/20260715-FIX-CDX-win2k-boot-to-desktop-goal`, and the preserved run
artifacts): 8 research readers → 8 page authors → 9 reviewers (per-page accuracy
vs the dossiers + cross-page consistency) → 8 fixers. Review found 42 findings
(1 blocker: an "interpreter-only" overclaim falsifying the June dynrec runs; 4
capture-45/46 timestamp conflations traced into the shared data file) — all
applied.

## Validation

- All Microsoft-visual exhibits are our own HTML/CSS/SVG recreations, captioned
  as such; no screenshots republished; no product key or key-adjacent content
  anywhere (grep-checked for key-shaped strings).
- Inline scripts + lib/data pass `node --check`; HTML tag balance checked; tab
  strips/aria-current/chapnav placement uniform; only contract-approved external
  URLs (Google Fonts).
- Rendered over local HTTP and visually verified (index, loop, cpu, desktop,
  catalog shelf count 11 → 12).

## Notes

- The mddosem endgame branch was un-integrated at authoring time; facts were
  read via `git show` from its tip (278544ac8). If its final ledger wording
  changes on integration, `desktop.html`/`w2k-data.js` may deserve a follow-up
  pass — the two-eyes closure of KILN-00004 is still pending on the mddosem side.
- Chrome extension was not connected this session, so interactives were
  exercised by code review + syntax checks rather than live clicks; a post-deploy
  click-through is a sensible follow-up.
