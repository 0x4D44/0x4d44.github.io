# 2026.07.13 — JRN — Overnight code-review pass 2 (news)

## What I did
Second bounded review pass of the day. Ledger showed only darmok (today) +
japanese-travel-rpg reviewed; everything else `(never)`. Picked `news/` (top
never-reviewed by risk — largest hand-written logic, and the deliberate pass the
darmok pass flagged). Read `news.js` (627) and `ads.js` (239) in full by hand — NOT
`/deep-review` (it retargets to the latest commit on an empty diff; lesson from pass
1). Ran corpus oracles. Filed 2 new bugs (KILN-00026, 00027). Upserted `news/` to
2026-07-13. Worktree: `D:/worktrees/0x4d44.github.io/20260713-CR-CLD-review-news`.

## Findings
- Re-verified the 7 bugs filed from pass-1's misfire (KILN-00018..00024) against the
  current `news.js` — all still valid, none fixed in the interim, no dupes re-filed.
- NEW: **homepage always duplicates a story** (KILN-00026) — the two feature bands
  filter the rotated sequence without excluding the top block; reproduced 48/48 hourly
  seeds by replicating news.js's own hash+shuffle. Deterministic, user-visible, low.
- NEW: **unguarded article-field access** (KILN-00027) — `a.category/headline/
  standfirst.toLowerCase()` in renderSearch aren't null-safe (unlike `esc()`), so one
  malformed appended article blanks the search/category/About page. Corpus is clean
  now (355 articles verified), so it's a latent robustness + test gap — matches the
  "no editorial oversight, just append an object" design the About page advertises.
- Report-only lows: related-cards regex-surgery (news.js:441), unbounded search
  results each with an inline SVG (549), empty-search builds the full haystack (506).
- Refuted: ad cross-promo links (all 7 slugs exist); duplicate SVG gradient id (each
  SVG has its own defs; browser resolves to first, visually identical).

## Surprises / decisions
- The corpus grew 255 → 355 between passes (other agents adding articles). My
  corpus-shape probe found it clean, which downgraded KILN-00027 from "live defect" to
  "latent gap" — correct call to verify against the real data rather than assume.
- Deliberately did NOT mint a second test-infra req for the news corpus oracle
  (darmok's KILN-00002 already established the pattern); folded the oracle
  recommendation into KILN-00027's fix notes instead. Conservative capture.
- Kept the pass lean per proportionality: 2 files read, corpus checks, 2 bugs. No
  fan-out needed — hand reading 627 lines + differential oracles was the right tool
  for a self-contained engine.

## Next
Next pass: **`onu/`** (never-reviewed, personality-flip logic, HLD 2026-07-11), then
tidecall, brilliancy, shipshape, span-of-control, cruise-line — all `(never)`.
