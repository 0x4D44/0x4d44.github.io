# 2026.07.13 — JRN — Overnight code-review pass 3 (onu)

## What I did
Third bounded review pass. Picked `onu/` from the ledger (top never-reviewed by risk).
Read `engine.mjs` (686) and `app.mjs` (692) in full, ran the test suite, and ran a
9000-game fuzz to extend the existing seeded sims. Filed 1 low bug (KILN-00030).
Upserted `onu/` → 2026-07-13. Worktree:
`D:/worktrees/0x4d44.github.io/20260713-CR-CLD-review-onu`.

## Key result: the slice is genuinely solid
- 29/29 existing tests pass (unit + property sims + headless-browser CDP test + a11y).
- My 9000-seed fuzz (3000 × classic/flip/chaos, resolving every request branch,
  asserting `assertState` + card conservation each transition) was 9000/9000 clean.
  No engine crash, hang, or invariant violation.
- So I did NOT manufacture findings. One low app-side bug + two report-only nits is
  the honest yield. Deliberately skipped a 7-lens workflow fan-out — a clean,
  well-tested slice doesn't warrant it (proportionality); the fuzz + full hand-read was
  the right, higher-signal tool.

## Findings
- KILN-00030 (Low): `app.mjs:46` reads `localStorage` at module top level with no
  try/catch, so a storage `SecurityError` (blocked cookies / sandboxed iframe) aborts
  the whole module → blank page. Inconsistent with the repo's guarded pattern (darmok).
- Report-only: `nextSeat` precedence (`engine.mjs:142`) — `direction*steps%count` is a
  latent foot-gun that's correct only because steps≤2<count=4 (verified across the fuzz).
- Report-only: jump-in offers only `candidates[0]` (`app.mjs:509`) — AI-vs-AI jump is
  seat-deterministic, not a race; human never denied. Likely intended simplification.

## Surprises / decisions
- My fuzz "failures" in chaos were all MY harness sending malformed stack/jump commands
  (missing cardId, or replaying a jumping wild's colour as `play` not `jump`). The
  engine correctly rejected them — a good sign, not a bug. Fixed the harness twice;
  after that chaos was 3000/3000. Lesson reinforced: when a fuzz "finds a bug," first
  suspect the driver, then the target.
- Confirmed the chaos "play your last card as a 7/0" winner-attribution (winner is
  whoever holds the empty hand AFTER the swap/rotate) is BY DESIGN — there's an explicit
  test ("derive the post-transfer winner") and the rules text says effects resolve first.
  Did not file it.

## Next
Next pass: **`tidecall/`** (hand-built vanilla-JS card game; lessons_learnt already
flags a render-flicker class there worth confirming against current code), then
brilliancy, shipshape, span-of-control, cruise-line — all `(never)`.
