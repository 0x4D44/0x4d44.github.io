    $("#stageSvgDesc").textContent = `${pkg.name}, body ${pkg.envelope}, over a ${coin.name} coin ${coin.diameter.toFixed(2)} millimetres in diameter.`;

    const span = spanOf(pkg);
    const ratio = span / coin.diameter;
    const nearest = nearestCoin(pkg);
    $("#packageEra").textContent = pkg.family;
    $("#packageName").textContent = pkg.name;
    $("#packageExample").textContent = `famous passenger: ${pkg.example}`;
    $("#packageBlurb").textContent = pkg.blurb;
    $("#bodyMetric").textContent = pkg.envelope;
    $("#coinMetric").textContent = `${coin.diameter.toFixed(2)} mm`;
    $("#ratioMetric").textContent = ratio < 1 ? `${Math.round(ratio*100)}%` : `${ratio.toFixed(2)}×`;
    $("#nearestMetric").textContent = `${nearest.label} (${nearest.diameter.toFixed(2)} mm)`;
