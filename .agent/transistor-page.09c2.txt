    $("#fitMetric").textContent = ratio < 1 ? `${(1/ratio).toFixed(1)} bodies` : `over by ${Math.round((ratio-1)*100)}%`;
    $("#verdictText").textContent = verdict(pkg, coin);

    $$("#packageRail .chip").forEach(b => b.setAttribute("aria-pressed", String(b.dataset.id === state.packageId)));
    $$("#coinRail .chip").forEach(b => b.setAttribute("aria-pressed", String(b.dataset.id === state.coinId)));
    $$("#viewModes button").forEach(b => b.setAttribute("aria-pressed", String(b.dataset.mode === state.mode)));
    $("#dimensionsButton").setAttribute("aria-pressed", String(state.dimensions));
    renderCoinSweep();
    renderActualSize();
  }

  function renderCoinSweep() {
    const pkg = pkgById(state.packageId);
    const fixedView = 48;
    const cards = COINS.map(coin => {
      const ratio = spanOf(pkg)/coin.diameter;
